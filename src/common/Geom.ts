/*export abstract class IPoint{
    constructor(public x: number | null = null, public y: number | null = null) {}
}

export abstract class ISize{
    constructor(public x: number | null = null, public y: number | null = null, public w: number | null = null, public h: number | null = null) {}
}

export class Point extends IPoint{ donotConfuseWithGraphPoint: boolean = true; }
export class GraphPoint extends IPoint{ donotConfuseWithPoint: boolean = true; }

export class Size extends ISize{ donotConfuseWithGraphSize: boolean = true; }
export class GraphSize extends ISize{ donotConfuseWithPoint: boolean = true; }
*/
export {}
