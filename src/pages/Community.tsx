import {Try} from '../joiner';
import {Dashboard} from './components';

function CommunityPage(): JSX.Element {
    return(<Try>
        <Dashboard active={'Community'}>
            <div>Empty page, still in progress.</div>
        </Dashboard>
    </Try>);
}

export {CommunityPage};
