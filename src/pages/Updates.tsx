import {Try} from '../joiner';
import {Dashboard} from './components';

function UpdatesPage(): JSX.Element {
    return(<Try>
        <Dashboard active={'Updates'}>
            <div>Empty page, still in progress.</div>
        </Dashboard>
    </Try>);
}

export {UpdatesPage};
