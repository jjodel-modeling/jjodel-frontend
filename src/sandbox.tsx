import React from "react";

export const uselessexport = {}
/*
**
class A{
    data: any;

    render(){
        let aa = <div className={'root operation'}>
            <div>op
                {this.data.type && <Select className={'operation-view'} obj={this.data.type.id} field={'type'} label={this.data.name} />}
                {!this.data.type && <div>type:{this.data.type}</div>}
                    {console.log("dataaa", this.data) && null}
                </div>
                </div>;
    }
}

*/
