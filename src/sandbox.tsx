export const fakeexport = {};
/*
let thiss: LClass = null as any;
let node: LGraphElement = null as any;
let tsx = <>
    // qui sono alla classe
    { node.subElements.filter( (subelement) => (subelement.model.className === DReference.name) )
        // qui itero i sotto-nodi delle reference
        .map( (dnoderef: LGraphElement) => ( (dnoderef.model as any as LReference).target.map( (targetN: LClass) => <Edge source={dref} target= {targetN.nodes} />)) ) }


    <Edges class={ (this.data as LClass).references.filter( r => r.upperbound>0)  }/>
</>;

class ComponentEdgeProps{
    source: DGraphElement;
    target: DGraphElement[] | DGraphElement
}
class ComponentEdgeSProps{ // solo scorciatoia, non veramente indispensabile mai.
    source: LModelElement[] | LModelElement;
}

*/
