# Sessione 2026-09-25 (chat C-2026-09-25-1030)

## Stato a fine sessione

Il passo 3 del simulatore è nel tronco e pushato: nucleo di Petri (ondata 3a), pannello e run-state sul nuovo nucleo (ondata 3b), vecchio step booleano cancellato. Merge `f10352a7b`, Status `7e06d5839`; `simulation-engine` riallineato con un fast-forward. Nel tronco è anche il percorso di apertura dei progetti (lane 0030, merge `74ca160e7`, Status `838442f5a`): un progetto che non si apre mostra una schermata d'errore e non può più essere sovrascritto da un salvataggio. Tronco `alfonso-frontend-jjtl` a `7e06d5839`, allineato con origin. Vitest 4620, typecheck 14 (insieme §17), build verde. `open-path` e `simulation-engine` restano locali (i loro commit sono su origin dentro i merge).

## Decisioni prese

- 2026-09-25, **R-SIM-27..33** (`e4506e78f`): terminazione quando tutti i token sono in F; `simTerminal` facoltativo (ribalta P-2026-09-24-1005, emenda R-SIM-9); cinque stati del run (Not started, Running, Terminated, Deadlock sui candidati con guardie valutate, Halted sticky con motivo); accessore `x.[marked]`/`x.[tokens]` quando arriva `.[x]`, intanto inibitori come congiunto `tokens(p) < w`; regole di compilazione (`else` letterale, arco senza target = difetto, fork/join non sono posti e si fondono); chiavi nuove (`simBound`, `simInitialMarking`, `simFork`, `simJoin`, `simGuard`, `simArc`, `simArcSource`, `simArcTarget`, `simArcWeight`, `simInhibitorArc`); tre ondate 3a/3b/3c. Spec §3.3 e §3.4 emendate. Conseguenze accettate: confluenze a k = 1 unsafe; exit ed entry sullo stesso attributo in un self-loop sono doppio assegnamento.
- 2026-09-25, **R-SIM-34..37** (`7a93968c5`): interruzione del run con firma limitata al run (6/6 misurati, `buildValidationSignature` 4/6); scelta fra candidati come lista, niente random fino alla traccia del passo 5; store `SimRun`, `simReset(modelId, run)`, `simCommit`, `getSimRun`, versione solo quando il marking cambia; precisione A: lo stato del pannello e «Last step» non dipendono dalla versione; lato M2 in quattro gruppi; chiavi di R-SIM-32 definitive.
- La lane 0030 (apertura progetti), nata in una chat non più rintracciabile (C-2026-09-25-0016), è stata adottata da questa chat.
- Il push di `open-path` e `simulation-engine` non serve: i commit arrivano con i merge `--no-ff`.

## Bug risolti

- **Perdita di dati all'apertura in pagina** (0030, `0609e9793`): se un'apertura falliva, lo store teneva lo stub della dashboard e un salvataggio sovrascriveva il progetto (19 voci a 7). Root cause: `Action.fire` rimanda il dispatch del LOAD con `setTimeout`, nessuna catch lo vede e `isLoading` non si chiude mai. Fix: LOAD sincrono, una sola catch in `stateInitializer`, `loadError`, guardia sul salvataggio, contatore delle aperture, evento `PROJECT_OPEN_CHANGED`. 12 mutanti uccisi.
- **Pagina bianca su 3001** (ambiente): il symlink permanente `~/jjodel-release/frontend/node_modules` era sparito (rimosso da una lane precedente che applicava P14 alla lettera) e la cache `.vite` condivisa era stata riscritta alle 00:32 dalla discovery 0030; ricreato il symlink dalla chat e riavviato Vite su 3001 (ora in background, log `/tmp/s3/vite3001.log`).

## Bug nuovi / Todo

- **Alta**: cambio di hash fra due progetti in pagina non avvia l'apertura; `Project.tsx` va in crash su `project.type` (ticket in `docs/log-inbox/versionfixer.md`).
- **Alta, harness**: P14 non distingue il symlink permanente di `node_modules` da quello temporaneo; la cache `.vite` sotto `~/jjodel/frontend/node_modules/.vite` è condivisa da tutti i worktree e un server avviato da una lane rompe gli altri. Va in `docs/PROTOCOL.md` con una lane che lo tiene (P15).
- **Media**: `pointedBy` cresce a ogni riapertura in pagina con salvataggio (0030).
- **Media**: `targetMetamodelId` nella validazione: da una scheda M1 un modello di un metamodello diverso dal primo si valida contro niente (ticket in `docs/decisions.md` accanto a R-SIM-33).
- **Bassa**: snellire `SimModelView`; rinominare `stcFromRoles.ts`; commenti obsoleti in `netStep.ts:22`, `netTypes.ts:5`, `:72`, `isKindOf.ts:17`, `guardEvaluator.ts:21`; vecchie sonde gitignorate del passo 1 non più valide.
- **Processo**: la skill `log-entry` chiede un commit separato per l'inbox, P13 un solo commit di chiusura; allineare la skill. Aprire anche i messaggi di avvio con `[P-...]`.
- Cinque entry nelle inbox (2 `simulation`, 3 `versionfixer`) aspettano il riporto P13.
- Da Alfonso: snippet di conteggio del `localStorage` (report 0030 §4) su 3000; conteggio lato server dei progetti con la firma senza `NODES_RECOMPILE_labels`.

## Documenti aggiornati

- `docs/decisions.md`: R-SIM-27..37 e due paragrafi Ticket.
- `docs/spec/claude_spec_2026-09-13_computational_model.md`: §3.3, §3.4, riga Amended.
- KB: `claude/spec_attive.md` (sezione simulatore e ticket di validazione).

## Prompt generati per Claude Code

- `P-2026-09-25-0935` Fase 2, ondata 3a: ✅ `79ee9fba3`, chiusura `0fc65866a`; 93 test, 36/36 mutanti; Status della Fase 1 chiusa dalla chat `2b8475d24`.
- `P-2026-09-25-1103` discovery 3b: ✅ `b9fd3a1f7`, Status `8c88f83f7`. Fase 2: ✅ `c400a5163` feat, `11de5af03` refactor, chiusura `4361f0fe1`; 48/48 mutanti; verifica visiva passata su 3002.
- `P-2026-09-25-0030` (adottato): Fase 2 ✅ `0609e9793`, chiusura `80599fb0f`, Status dalla chat `bd8a29bb9`; verifica visiva passata su 3003.
- `P-2026-09-25-1115` merge `open-path`: ✅ `74ca160e7`, Status `838442f5a`; vitest 4533; verifica visiva passata su 3001.
- `P-2026-09-25-1247` merge del passo 3: ✅ `f10352a7b`, Status `7e06d5839`; vitest 4620; verifica visiva passata su 3001; fast-forward di `simulation-engine`.

## Prompt pendenti

Invariati: `P-2026-09-18-1930` split di `CLAUDE.md`; ticket JjScript L5 (`P-2026-09-18-2055`). Lane `harness-bypass` (`P-2026-09-25-1022`, altra chat) chiusa in `~/jjodel-gate` ma non fusa nel tronco.

## Prossimi passi

1. Riporto P13 delle cinque entry nelle inbox (corsia esclusiva).
2. Lane harness su P14: symlink permanente di `node_modules` e cache `.vite` per worktree.
3. Ticket alto sul cambio di hash fra due progetti (nuova discovery in un worktree dal tronco).
4. Simulatore: corsia dell'operatore `.[x]` con i tipi `Expression` e `Action` (discovery con Layer Impact Report), poi ondata 3c (candidati sul canvas, critical zone), poi passo 4 (snapshot).
5. Merge di `harness-bypass` quando la sua chat lo decide.

## Info strutturali scoperte

- Il nucleo del simulatore è in `frontend/src/model/simulation/` (`netTypes.ts`, `netCompile.ts`, `netStep.ts`); il bridge in `components/editor-v2/sim/simBridge.ts`, con `buildEvalContext` iniettato e `targetMetamodelId` sempre passato. `ObjectNode.tsx` e `viewpoint/ir/*` leggono il marking solo tramite `isSimActive`/`useSimVersion`.
- Il costo di un Reset sul tornello è 3 ms (quasi tutto `buildEvalContext`), 15 ms a 74 oggetti.
- `Action.fire` rimanda il dispatch con `setTimeout(0)` o lo accoda alla transazione aperta; `store.dispatch` di redux 5 rilancia in modo sincrono.
- Da una scheda M1 `getActiveMetamodel()` è `null` per costruzione (`activeArtifact.ts:67`) e `getTargetMetamodel` ripiega sul primo metamodello.
- Il server su 3001 (`~/jjodel-release`) e i worktree prendono `node_modules` da `~/jjodel/frontend/node_modules` tramite symlink permanenti; la cache `.vite` sta lì ed è condivisa.
- Lo script degli scenari della 3b è in `~/jjodel-sim/frontend/scripts/smoke/_tmp_sim3b_scenarios.js` (gitignorato).

## Cronologia

La sessione parte dal punto sul simulatore e scopre che un'altra chat aveva già mosso il passo 3 fino alla discovery. Dal report si ratificano R-SIM-27..33 e si esegue la 3a (nucleo puro, parità misurata). In parallelo si adotta la lane 0030 rimasta orfana: la Fase 2 era già in corso, si accettano tre aggiunte coperte da mutanti e la verifica visiva passa. Si scrivono discovery e Fase 2 della 3b, ratificate come R-SIM-34..37 con due precisioni; codice, test e verifica su 3002 passano. Si fondono nel tronco prima `open-path` (conflitto d'inbox risolto per costruzione) e poi il passo 3. La verifica del primo merge inciampa su 3001: symlink sparito e cache `.vite` riscritta, sistemati dalla chat. Push del tronco a `7e06d5839`.
