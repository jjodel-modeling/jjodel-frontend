# Sessione 2026-08-23: il travaso, la slice 2b, e due diagnosi ritrattate

**Superficie**: Cowork con `~/jjodel` connessa. **Due novità di superficie**: dal bridge **si committa**
(il blocco non era il commit, era `.git/index.lock` che il bridge non cancella; `mv` funziona dove `rm`
non funziona, e serve `git --no-optional-locks`). E l'architetto ha eseguito **verifica in pagina via
automazione browser** sul Chrome di Alfonso. Push escluso, resta ad Alfonso.
**Branch**: `alfonso-frontend-jjtl`. **HEAD a fine sessione**: `210ecb232` (aggiornamento serale; la
versione precedente di questo file era ferma a `556737e96`).
**Questo checkpoint sostituisce** `sessione_CORRENTE.md` (versione 2026-08-21).

**Copre anche la sessione del 2026-08-22**, mai checkpointata: il suo file non è mai esistito. Quella
parte è **ricostruita dalle entry di log e dai quattro addendum di discovery**, non dalla chat.

---

## Stato a fine sessione

Il fronte layout ha le sue premesse nel repo, e la slice 2 di `2.228` ha il commit 2b chiuso con
verifica visiva. Il percorso critico del fronte layout è avanzato di un passo, ma **si è anche
allargato**: la misura in pagina ha mostrato che le sorgenti del viewpoint attivo sono tre, non due.

| Commit | Contenuto |
|---|---|
| `13b69dc76` … `de9c15b3b` | i quattro addendum di discovery del 22/8, tre arresti |
| `d0f4bf5fb` | **la serie `R-LAY-1..10` in `docs/decisions.md`** |
| `2a69f4358` | memo di ratifica e prompt delle 17:05, messi a terra |
| `5a2906c73` | §8 dichiarato perduto, D10 riformulata |
| `62d207664` | checkpoint di sessione (prima versione) |
| `b6357101b` | verifica visiva 2a passata, prompt del commit 2b |
| `fc9f8afda` | **il 2b: `activeViewpoint` vuoto a `null`, adapter `2.227 -> 2.228`** |
| `c7455439d` | riferimento rotto corretto alla fonte |
| `474e31d75` | verifica visiva 2b parziale, la terza sorgente di R-LAY-10 |
| `c2cbe814f` | **ritrattazione**: il difetto Save non esiste |
| `556737e96` | verifica visiva 2b dichiarata chiusa, prova 4 non verificata |
| `9971ba29c` | checkpoint di sessione, seconda versione |
| `57e06ca14` | **R-LAY-11**: la terza sorgente (`lastEditedViewpointId`) è morta per misura |
| `052966df8` | i due `select` di `NestedView.tsx` instradati su `activateViewpoint` (R-LAY-10/11) |
| `4c9953fe2` | VIEW e Theme inerti mentre un viewpoint è attivo; verifica visiva passata (sera) |
| `9ca37508d` | **R-LAY-12**: `NestedView` non è montato da nessun sito; verifica di R-LAY-11 sostituita dalla misura |
| `99a29b4e8` | **fronte R-DEAD aperto** (R-DEAD-1..6): perimetro di rimozione di `NestedView` |
| `08fde5e24` | entry di `4c9953fe2` a `Regressions: no` |
| `210ecb232` | **R-IRN-27: il commit 2c decade, slice 2 di `2.228` chiusa**; R-IRN-25 rettificata |

**Stato serale.** La condizione di R-LAY-10 (una sola sorgente dell'attivazione) è soddisfatta:
`activateViewpoint` è l'unico scrittore vivo e raggiungibile di `project.activeViewpoint` e
`state.viewpoint`. La slice 2 di `2.228` è chiusa senza 2c. **R-LAY-7 è sbloccata**: il fronte layout
riparte dal prompt delle 17:05, da riallineare prima del lancio.

---

## Decisioni prese

**La serie `R-LAY-1..10`** (ratificata il 22/8, iscritta il 23/8). Apparato e tracciabilità in
`docs/ratifiche/claude_2026-08-22_memo_ratifica_layout_per_viewpoint.md`. In sintesi: posizione
persistita **per viewpoint esclusivo** (R-LAY-1); sintassi astratta con record proprio (R-LAY-2);
**R-2 del 2026-08-03 emendata** nella sola clausola citata, decisione del 19/7 su `irEdgeLayout` e
`irCollapsed` intatta (R-LAY-3); chiave = **id del viewpoint esclusivo attivo**, non l'insieme di ciò
che rende (R-LAY-6, chiude D9); perimetro `editor-v2` con il classico governato e non esente
(R-LAY-9); ordine e divieto in R-LAY-7 e R-LAY-10.

**L'addendum §8 del memo è perduto e si chiude così** (23/8). Clausola scritta nel memo: chi trova
«l'addendum §8» in un RIFERIMENTI sta citando un documento inesistente, lo dichiara e procede sul
resto; se una decisione poggiava su §8 e su nient'altro, **va rifatta, non ricostruita**.

**D10 riscritta da capo** (23/8). Copre i **due casi limite della chiave**: D10.a la sentinella della
sintassi astratta (`null` non è una chiave di dizionario; cercare collisioni con id reali), D10.b la
sopravvivenza del record quando l'elemento smette di rendere. Dichiarata come riformulazione, non
come recupero.

**Verifica visiva del 2b: solo in Advanced** (Alfonso, 23/8). In Basic `Create View` non è esposta
(`ContextMenu.tsx:486`, gate `isAdvancedMode()`), quindi la prova 3 non è riproducibile lì; le prove
1, 2 e 4 girano su load e adapter e la modalità non le tocca.

**La verifica visiva del 2b è chiusa con un residuo dichiarato** (Alfonso, 23/8): prova 4 **non
verificata** sul percorso reale, non scritta come passata.

**R-LAY-11** (23/8). La terza sorgente osservata a schermo, `lastEditedViewpointId`
(`lastViewpoint.ts:15`), non è una sorgente dell'attivazione: `setLastEditedViewpoint` e
`clearLastEditedViewpoint` hanno zero call site, chiamanti rimossi con l'editor v3 in aprile. I tre
gate che la leggono (`ContextMenu.tsx:487`, `:531`, `TreeViewContent.tsx:483`) sono affordance
permanentemente disabilitate: **difetto UX distinto, fronte suo**, non bloccante per il layout.

**R-LAY-12** (23/8). `NestedView` e `NestedViewConnected` hanno zero consumatori (unico riferimento un
re-export in `editors/index.ts:8` che nessuno importa). I tre gesti di attivazione del pannello erano
irraggiungibili: la divergenza «già viva» era viva nel codice, non a schermo. `052966df8` si tiene come
hardening difensivo. Verifica visiva del prompt delle 16:47 sostituita dalla misura a livello di codice
più l'unico gesto vivo (select della toolbar).

**R-DEAD-1..6** (23/8, su richiesta di Alfonso). Fronte di rimozione di `NestedView`, in tre slice
(R-DEAD-5): slice 1 cancella `views/NestedView.tsx` e la riga 8 di `editors/index.ts`; slice 2 la
cascata esclusiva (`GenericTree`, `InternalToggle`, `LockedFeature`, file ospitanti intatti, R-DEAD-3);
slice 3 candidata, non deliberata. `nestedView.scss` **resta** (importato da `ViewData.tsx:24`, R-DEAD-2).
Metodo vincolante: prima di cancellare un simbolo, verificare che non sia esportato da
`joiner/components.tsx` (R-DEAD-4). DS-3 del 5/8 non è eseguibile come scritta (R-DEAD-6).

**R-IRN-27** (23/8, «vai» di Alfonso). Il 2c decade: la premessa di R-IRN-22 è falsa per misura, il
bottone di `NestedView.tsx:397-400` non può apparire; `updateDefaultView` ha un solo consumatore vivo
(`VersionFixer.tsx:141`), la deroga di R-IRN-24 resta non esercitata, `Defaults.ts` non si tocca. Non
si scrive nemmeno come hardening: R-DEAD slice 1 cancella il file. **R-IRN-25 rettificata** nella
sola parentesi: il tab «Viewpoints» lo rende `Info.tsx:1341-1356` (`ViewpointProperties` /
`ViewData`), non `NestedView`; i commenti di `TabDataMaker.tsx:6-7` e `:36-39` sono stale.

---

## Bug risolti

**`.git/index.lock` non cancellabile dal bridge.** Il bridge non può fare `unlink`, quindi ogni
`git status` normale lascia un lock che blocca i commit dal Mac. Rimedio: `mv` invece di `rm`,
`git --no-optional-locks` per le letture, identità passata a mano (`-c user.name` / `-c user.email`:
`~/.gitconfig` sta fuori dal mount). **Conseguenza: dal bridge si committa.**

**Il commit 2b** (`fc9f8afda`): default di `activeViewpoint` a `null`, getter senza fallback,
`newDefault` riscritto (R-IRN-23), adapter `2.227 -> 2.228` più il suo test. Gate verdi, typecheck
identico byte a byte alla baseline (33).

---

## Le due diagnosi ritrattate, e perché contano più dei fatti

**«Il comando Save non salva».** Scritto a registro come difetto del prodotto sulla base di un
osservabile sbagliato: la lunghezza del blob compresso, che non cresceva. Alfonso ha eseguito la
stessa procedura a mano e il blob cambia (13791 → 13788 → 13798). Ritrattato in `c2cbe814f`.

**«Il salvataggio non è avvenuto, quindi il viewpoint non c'è».** Smentito da Alfonso con
un'obiezione di una riga, e poi dalla misura: dopo il reload `model_1`, `VP prova` (stesso id) e le
due view **sopravvivono tutte**. Il blob passa da 13791 a 13798 byte pur contenendo sei record in
più, perché lz-string comprime strutture ripetitive quasi a costo zero. **La dimensione compressa non
è un osservabile di contenuto.**

**Regola che ne segue, iscritta nell'entry di log**: un comportamento osservato **solo** attraverso
l'automazione non è un difetto del prodotto finché un umano non lo riproduce a mano; per quel genere
di osservazione il campo `Causa` è `(g)`, ostacolo ambientale.

---

## Bug nuovi e todo

**Alta**

1. **Riallineare e rilanciare il prompt delle 17:05** (D1..D8 + D10): è del 22, precede R-LAY-11,
   R-LAY-12 e R-DEAD, e cita `NestedView.tsx` come secondo writer. Tabella di premesse in testa,
   come per il 2b. Read-only, indipendente da `2.228`.
2. **Fronte UX dei gate «Create View»** (`TreeViewContent.tsx:483`, `ContextMenu.tsx:487`, `:531`):
   leggono una variabile che nessuno scrive più. Rimedio da decidere (gate su `state.viewpoint`?),
   fronte suo per R-LAY-11.
3. **Le tre clausole di processo non sono iscritte.** Numeri liberi: `RC-8`, `RC-9`, `RC-10` (la serie
   in `decisions.md` ha `RC-3`, `RC-4`, `RC-7`; 1, 2, 5, 6 non esistono in nessun file del repo).

**Media**

4. **Cinque esecuzioni dell'adapter per boot.** Al boot l'adapter `2.227 -> 2.228` normalizza cinque
   volte, ogni volta ripartendo da uno stato in cui il campo è ancora `Pointer_ViewPointDefault`.
   `VersionFixer.update` ha un solo call site di produzione (`SaveManager.ts:56`). Navigando dentro la
   SPA le invocazioni sono **zero**: il fenomeno è confinato al bootstrap. Fuori perimetro del 2b,
   nessun effetto sulla correttezza misurata. Primo posto dove guardare se il boot diventa lento.
5. **Prova 4 non verificata**: idempotenza sul percorso `SaveManager.load`. Coperta dal test unitario,
   non da un gate. Si chiude in due minuti: salvare, ricaricare, guardare se la riga del VersionFixer
   compare.
6. **Il quick tip annuncia «Press Ctrl+S», il menu espone `⌘S`.** Su macOS `ctrl+s` non salva.
7. **La entry di log di `363e121c0` (2b-i) manca.** Lacuna P9 arretrata, da scrivere alla rotazione.
8. **Rotazione del log dovuta**: 50 entry attive contro soglia 40.
9. **Le custom instructions del Project divergono dal repo** (rotazione log 20 contro 40,
   `localhost:3001` contro 3000). Invariato dal 21/8. Solo Alfonso può cambiarle.
10. Backlog ereditato: `forms.scss` fuori dal sistema dei token; tema scuro sospeso ma selezionabile;
    archi 5, 6, 7 di D-UI-13; serie D mai arrivata in `decisions.md`.

---

## Documenti aggiornati

- `docs/decisions.md`: `R-LAY-1..10` (righe 1675-1693).
- `docs/ratifiche/claude_2026-08-22_memo_ratifica_layout_per_viewpoint.md`: **nuovo**. Dieci righe
  verbatim, tabella di tracciabilità riga → finding, §8 chiuso come perduto.
- `docs/prompts/claude_2026-08-22_1705_prompt_layout_per_viewpoint_d1_d8_d10.md`: **nuovo**,
  ricostruito; D10 riscritta.
- `docs/prompts/claude_2026-08-23_1425_prompt_2228_slice2_commit2b.md`: **nuovo**, con tabella di
  riallineamento delle premesse del prompt del 18.
- `docs/discovery/discovery_2026-08-23_2228_slice2b_riallineamento.md`: **nuovo** (Claude Code).
- `frontend/src/`: `joiner/classes.ts`, `view/viewElement/view.tsx`, `redux/VersionFixer.tsx`,
  `redux/__tests__/versionfixer_2228_migration.test.ts` (nuovo).
- `docs/claude-code-log.md`: entry del 2b, verifica visiva 2a e 2b in place, ritrattazione, backlog.
- **Sera**: `docs/decisions.md` (R-LAY-11, R-LAY-12, R-DEAD-1..6, R-IRN-27, rettifica R-IRN-25);
  `docs/discovery/discovery_2026-08-23_rlay10_verifica_premesse.md`,
  `discovery_2026-08-23_nestedview_ui_morta.md`, `discovery_2026-08-23_perimetro_rimozione_nestedview.md`
  (Claude Code); `frontend/src/components/editors/views/NestedView.tsx` (3 righe), `editor-v2/Toolbar.tsx`,
  `components/ColorSchemeSelector.tsx`, `EditorV2.scss`.

## Prompt generati per Claude Code

| Prompt | Esito |
|---|---|
| `claude_2026-08-22_1705_prompt_layout_per_viewpoint_d1_d8_d10.md` | ⚠️ arresto al passo zero il 22; **ora a terra, premesse presenti, da rieseguire** |
| `claude_2026-08-23_1425_prompt_2228_slice2_commit2b.md` | ✅ `fc9f8afda`, gate verdi, verifica visiva chiusa |
| `claude_2026-08-23_1647_prompt_rlay11_sorgente_unica_attivazione.md` | ✅ `57e06ca14` + `052966df8`; verifica visiva sostituita da R-LAY-12 |
| prompt delle 21:39 (raggiungibilità di `NestedView`) | ✅ `9ca37508d`, R-LAY-12 |
| «apri un fronte per rimuovere NestedView» | ✅ `99a29b4e8`, R-DEAD-1..6 |

## Prompt pendenti

`claude_2026-08-18_1656_prompt_2228_fase2.md` **non è più pendente** (2c decaduto, R-IRN-27). Invariati:
`claude_2026-08-19_2336_prompt_ui_B_palette_rail_sinistro.md`,
`claude_2026-08-20_0025_prompt_ui_C_fase2_property_editor.md`,
`claude_2026-08-14_1530_prompt_J1_walker_jjel_modulo_puro.md`.

---

## Prossimi passi

1. **Push** dei commit serali (`08fde5e24`, `210ecb232` e quanto non ancora pushato).
2. **Riallineare il prompt delle 17:05** a R-LAY-11, R-LAY-12, R-DEAD; poi lanciarlo (read-only).
3. **R-DEAD slice 1** (due file, corsia veloce) quando il repo è fermo; non prima del discovery del
   layout, per non incrociare due sessioni di Claude Code sullo stesso albero.
4. **Fronte UX «Create View»**: decisione sul gate, poi prompt piccolo.
5. **`RC-8`, `RC-9`, `RC-10`**, entry arretrata di `363e121c0`, rotazione del log (50 > 40).

---

## Info strutturali scoperte

- **Dal bridge Cowork si committa** con `git --no-optional-locks` e l'identità passata a mano; i lock
  si neutralizzano con `mv`. Il push resta escluso.
- **Il selettore di viewpoint su un metamodello mostra solo «Abstract syntax» per design**
  (`Toolbar.tsx:224`, «On a metamodel the list is not rendered»). Serve un **modello** per esercitarlo.
- **Con i tab tutti montati (R-A), `document.querySelector('select')` prende quello del tab nascosto.**
  Filtrare per visibilità, o si misura la superficie sbagliata.
- **`Defaults.viewpoints` ha un solo elemento**, `Pointer_ViewPointDefault`: l'unico viewpoint di
  sistema, ed è una costante, non un id generato.
- **In offline i progetti stanno in `localStorage.projects`**, ciascuno con `state` compresso
  (`compressToUTF16`). Un progetto mai salvato ha `state` vuoto e viene costruito dal DTO, che è
  perché «test» portava `activeViewpoint: Pointer_ViewPointDefault` pur essendo vuoto.
- **Più schede sulla stessa origine condividono `localStorage`**: l'ultima che salva vince. Trappola
  per qualunque verifica in pagina.
- **La navigazione SPA con solo cambio di hash non ricarica**, quindi non riesegue il bootstrap: un
  «reload» fatto così è un falso negativo. Serve `location.reload()`.
- **I lock di `.git`** (`HEAD.lock`, `next-index-N.lock`) lasciati da un commit dal bridge a volte non
  si spostano nemmeno con `mv`: li cancella Alfonso con `rm -f`. Il commit riesce comunque.
- **Il tab «Viewpoints» del pannello classico è reso da `Info.tsx:1341-1356`**, non da `NestedView`.
- **Il push dal bridge è bloccato dal proxy** (403 su CONNECT), non da git.
- `handleAutoLayout` è a `EditorV2.tsx:3249`, non a `:3262`. `useContentSize.ts` sta in
  `frontend/src/components/editor-v2/viewpoint/ir/`.

---

## Cronologia

La giornata si apre con quattro verifiche chieste da Alfonso e con l'istruzione di non assumere lo
stato del repo. Tre su quattro correggono l'aspettativa: la serie `R-LAY` era stata iscritta la
mattina stessa, il memo no, e **nessuno** dei quattro prompt del 22 era mai entrato nel repo.

Il travaso occupa la prima metà. Memo e prompt si ricostruiscono dal registro; §8 è perduto e viene
dichiarato tale con una clausola su come comportarsi quando lo si trova citato; D10 viene riscritta da
capo. Poi il 2a viene verificato a schermo, il 2b spedito ed eseguito, e il suo report trova il
finding che conta: la divergenza fra i due scrittori dell'attivazione è **viva**, e i gesti che la
producono sono tre, non due.

La seconda metà è verifica in pagina, ed è la parte istruttiva. Le prove 1, 3 e 5 passano con
osservabili solidi. Ma l'automazione produce anche **due diagnosi sbagliate**, tutte e due dello
stesso tipo: una misura comoda scambiata per la misura che decide. Prima «il Save non salva», dedotto
da una lunghezza compressa che non cambiava. Poi «il salvataggio non è avvenuto», smentito da Alfonso
con un'obiezione di una riga — *se non avevi salvato, il viewpoint non ci sarebbe* — e poi dalla
misura. Entrambe erano finite a registro, ed entrambe sono state ritrattate lo stesso giorno.

Il guadagno vero della verifica in pagina non è nessuna delle cinque prove: è la **terza sorgente**
del viewpoint attivo, vista per caso provando due volte lo stesso menu contestuale in due stati
diversi. Il rimedio di R-LAY-10 che il report proponeva ne chiude due su tre, e questo si sapeva solo
guardando lo schermo.

La lezione della giornata è la stessa del 22 vista da un altro lato. Il 22 il processo si era fermato
tre volte perché un gate chiedeva una misura che non c'era. Oggi è andato avanti tre volte su una
misura che c'era ma non diceva quello che sembrava dire. **Il gate protegge dall'assenza di misura,
non dalla misura sbagliata**; contro quella l'unica difesa che ha funzionato è stata una domanda di
Alfonso in una riga.

**Sera.** Il piano dell'ora parte da una premessa sbagliata dell'architetto: il file di sessione era
fermo a `556737e96` e nel frattempo Claude Code aveva chiuso R-LAY-11, misurato che `NestedView` non
è montato (R-LAY-12) e aperto R-DEAD su richiesta di Alfonso. La misura ha svuotato il 2c: il difetto
visibile che R-IRN-22 voleva prevenire non è visibile, e la deroga di R-IRN-24 proteggeva una
divergenza senza secondo consumatore. R-IRN-27 lo mette a registro e chiude la slice 2 senza codice.
Lezione, la stessa della giornata: la sessione del coder e la sessione dell'architetto vedono lo
stesso repo solo se si legge `git log` prima di pianificare, non il checkpoint.
