# Sessione 2026-09-05 / 2026-09-06 — Tutorial docs 1-4 con video pill, bug JjTL contenimento

Chat Cowork collegata al Mac (cartelle `~/jjodel-docs` e, dal 6/9, `~/jjodel`). Commit sui due repo dalla
shell nativa del Mac via osascript (nessun lock); push di `jjodel-docs` su `main` eseguito (sito online).
Branch frontend `alfonso-frontend-jjtl`. Chiave OpenAI usata per Jjodie: solo in `.env.jjodel` dello
scratchpad cloud e nel browser di registrazione (rimossa); da ruotare.

## Stato a fine sessione

**docs.jjodel.io** (main `5c15c11`, poi commit di Alfonso): sezione Tutorials rifatta su un solo esempio ER.
Percorso: 1 metamodello ER (video), 2 Chen con View Designer (video), 3 Data Manager (video), 4 Jjodie
(video), 5 trasformazione ER→relazionale (planned), 6 state machine simulation (vecchio T4, ancora su
JSX/CSS 1.5, da aggiornare). Tolta la tappa «seconda notazione + validazione». Sezione Video Tutorials
rimossa. Pipeline video in `scripts/video-pills/` (Playwright headless + cursore sintetico + Piper Lessac +
ffmpeg; `compose.py` con tagli dei tempi morti, fast-forward `ffStart/ffEnd`, titolo sovrapposto in post).

**Frontend** (`ad963f752`): JjTL ora enumera le istanze contenute, crea oggetti annidati in feature di
contenimento (`-> feature { forall … -> Class {…} }` e `-> feature { -> Class {…} }`), dereferenzia
`{__ref}` nel forall, accetta chiavi identificatore nel value mapping e newline prima di `->`. Non ancora
deployato su beta.

**Progetto `ERDLanguage` su beta**: ERD (con `isKey`), Relational, Library (Catalogue/Book/Member/Loan),
People a 3 entità/2 relazioni, ChenNotation (3 view + fill condizionale), trasformazione `ER_to_Relational`
salvata. Il modello cresciuto del tutorial 3 (6 entità, 5 relazioni) è stato tolto per registrare Chen.

## Decisioni prese

- 5/9: video brevi (<3 min) e supplementari al testo; registrare su beta con account di Alfonso; testi
  con app.jjodel.io; logo bianco all'inizio, voce Lessac, tagli dei tempi morti, video nel repo.
- 5/9: Chen si fa con il View Designer (Applies to / Structure / Symbol), niente JSX/SCSS.
- 6/9: rinumerazione: Data Manager è 3, Jjodie è 4; tolta «seconda notazione».
- 6/9 (GO JjTL, `docs/prompts/claude_2026-09-06_1520_go_…fase2.md`): D1 `-> feature {…}` è una feature
  target risolta sul metamodello, dentro sia `-> Class` sia `forall`; D2 forall a livello di regola resta con
  lookup della feature e euristica come fallback con warning, `-> Class` a livello di regola è errore;
  D3 `{__ref}` dereferenziato nel forall e in `parent`; D4 enumerazione dei contenuti sempre; D5 write-back
  fuori dalla TRANSACTION di STEP 6, figli risolti per id (deviazione accettata); D6 niente no-op muti;
  D7 enum risolti a `DEnumLiteral` nel write-back (deviazione accettata).

## Bug risolti

- JjTL: istanze contenute invisibili (`ProjectEditor` usava `sourceModel.objects`, solo radici) → `f428e1470`.
- JjTL parser: newline prima di `->` nel forall; `String=VARCHAR` nel value mapping → `63131b0fb`.
- JjTL: creazione annidata mai scritta come DObject (valore d'attributo scartato da STEP 6) → `cde18558b`,
  `3d6b16e14`.

## Bug nuovi / Todo

1. Frontend: export progetto (`Download` da sidebar e da card) produce `.jjodel` con `state` vuoto. Alta.
2. Frontend: salvataggio last-write-wins tra due sessioni; Ctrl+S seguito da chiusura perde il viewpoint;
   «Request timed out» sul save sotto carico. Alta.
3. Frontend: 7 file di test jjtl rossi all'import (`window is not defined`, barrel `joiner` per `U.asNumber`);
   `.forAll(x: pred)` non parsa mai sul percorso app (lexer minuscola prima del lookup, `useJjtlParser` senza
   `source`). Media.
4. Frontend: commit `3d6b16e14` senza test automatico (debito dichiarato). Media.
5. Frontend UI: Delete di metamodelli/viewpoint dal menu card senza conferma; multi-edit DM `type` mostra
   `Mixed (Pointer…)`; area tabella DM non cresce con la riga espansa; Auto layout Chen su una riga larghissima;
   pannello destro copre il symbol editor nelle regole condizionali; rename in header non persiste; import
   appende « copy». Media/bassa.
6. Jjodie/doc: manca il bottone «Ask Jjodie» descritto in `ai/jjodie.md`; blocchi `@protected` raccolti sotto
   Notes dopo Regenerate e Notes di default perso; generatore locale scrive «extends undefined»; la
   documentazione parte in locale anche con provider configurato (scelta modello per feature). Media.
7. Docs: `languages/jjtl.md` da aggiornare dopo il deploy (forall in feature, contenuti enumerati, value
   mapping con identificatori, limiti corretti); `ai/jjodie.md` (Ask Jjodie); `tutorial-04-simulation.md`
   ancora sulla 1.5. Media.
8. Docs: tutorial 5 (ER→relazionale) da scrivere e registrare quando beta ha `ad963f752`. Alta.

## Documenti aggiornati

jjodel-docs: `tutorials/index.md`, `01-er-metamodel.md`, `02-chen-notation.md`, `03-data-manager.md`,
`04-jjodie.md`, immagini `tutorial-0N-*.png`, `public/videos/tutorial-0{1,2,3,4}-*.mp4/.jpg`,
`scripts/video-pills/*`, `src/assets/jjodel-logo-white.png`, `.gitignore`, `astro.config.mjs`, `faq.md`,
`concepts/domain-analysis.md`. Frontend: `docs/prompts/claude_2026-09-06_1440_…`, `…_1520_go_…`,
`docs/discovery/discovery_2026-09-06_jjtl_contained_sources_nested_creation.md`, `frontend/src/jjtl/SPEC.md`,
`jjtl/CLAUDE.md`, `jjtl/AGENTS.md`, `docs/claude-code-log.md`.

## Prompt generati per Claude Code

- `claude_2026-09-06_1440_prompt_jjtl_contained_sources_and_nested_creation.md` → ✅ referto `985eb7608`.
- `claude_2026-09-06_1520_go_jjtl_contained_sources_and_nested_creation_fase2.md` → ✅ `f428e1470`,
  `63131b0fb`, `cde18558b`, `3d6b16e14`, `454773e8b`, `ad963f752`.

## Prompt pendenti

Nessuno. Da scrivere: fix export vuoto (todo 1), test jjtl rossi all'import e `.forAll` (todo 3).

## Prossimi passi

1. Deploy di `alfonso-frontend-jjtl` su beta; verifica di `ER_to_Relational` su `People` (3 Table con 3/2/2
   Column, chiavi su id2/id3, tipi SqlType, 2 ForeignKey).
2. Tutorial 5: testo (~1000 parole), screenshot, video pill; regola completa con `type := a.type :
   String=VARCHAR, Integer=INTEGER, Boolean=BOOLEAN` e `isPrimaryKey := a.isKey`.
3. Aggiornare `languages/jjtl.md`, `ai/jjodie.md`, `user-guide/transformation-editor.md` (Trace/warning).
4. Ricrescere il modello People (Department/Project/Address, 3 relazioni) se il tutorial 5 lo richiede.
5. Ruotare la chiave OpenAI.

## Info strutturali scoperte

- Beta: chiavi provider in localStorage (`jjodie_provider_gpt`, `jjodie-credentials`); documentazione in
  `jjodie_doc_<projectId>`; tab dock non persistono al reload; Ctrl+S dal project page non salva.
- Selettori utili (3.0 beta): viewpoint `[title="Select viewpoint"]` → `.toolbar-viewpoint-menu`; DM
  `.instance-manager__*` (draft `#instance-manager-draft-<feature>`, delete `.instance-manager__del`);
  Jjodie `.jodie-minimized`, `.jodie-input`, `.jodie-send-btn` (`--no-provider` apre Settings),
  `.exec-error-overlay`; Explain `.explain-modal`; doc `.provider-btn`/`.provider-option`,
  `button[title="Edit documentation"]`, Monaco via `window.monaco`; transformation editor Monaco idem,
  `button:has-text("Validate"|"Execute")`, output `Execute Transformation`.
- Container cloud a 2 vCPU: l'app gira a ~15 fps, ogni click Playwright ~1 s; da qui i fast-forward.
- Da bridge VM niente git che tocchi l'indice; dalla shell nativa (osascript `do shell script`) tutto ok.

## Cronologia

5/9: rimozione Video Tutorials; nuovo indice tutorial; tutorial 1 riscritto sulla 3.0 con pilot video,
poi logo/Lessac/poster; tutorial 2 Chen sul View Designer; tutorial Data Manager con video; scoperta limiti
del JjTL executor. 6/9: rinumerazione (DM=3, Jjodie=4); video Chen; tutorial 4 Jjodie con chiave OpenAI;
merge su main e pubblicazione; repo frontend collegato, root cause JjTL, prompt two-phase, referto,
GO con sette decisioni, sei commit, chiusura Fase 2.
