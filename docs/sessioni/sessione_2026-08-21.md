# Sessione 2026-08-21: la serie dei token consegna, un arco nasce e muore, e la cerimonia va tarata

**Superficie**: Cowork con `~/jjodel` connessa (l'architetto scrive su disco, ma **non puo' committare**:
il bridge non riesce a cancellare i lock di git, quindi i commit li chiude Alfonso dal Mac), Claude Code
su VS Code per l'esecuzione, misure in pagina dell'esecutore.
**Branch**: `alfonso-frontend-jjtl`. **HEAD a fine sessione**: `19df97e3a`, pushato su origin.
**Questo checkpoint sostituisce** `sessione_CORRENTE.md` (versione 2026-08-20 (3)).

---

## Stato a fine sessione

La serie cromatica ha consegnato il suo pezzo visibile. **Per la famiglia del testo esistono ora due
regimi invece di tre**, e al boot le didascalie sono leggibili per la prima volta.

| Commit | Contenuto |
|---|---|
| `9139887f1` | **arco 1bis**: le ultime due linee del rail sul token panel-border (D-UI-11) |
| `f89e625b8` | log dell'arco 1bis |
| `24e2dc3a6` | i tre discovery report del 19/8, archiviati |
| `4e6877376` | D-UI-13 Emendamento 3, prompt dell'arco 2 |
| `c2111b91a` | D-UI-13 Emendamenti 4 e 5, **arco 2 archiviato**, prompt dell'arco 4 |
| `85c4398f6` | **arco 4**: 47 scambi di token in 24 file, piu' la correzione del README |
| `66085dda2` | log dell'arco 4, e rotazione del log a 38 entry attive |
| `e17bf3c1e` | D-UI-13 Emendamento 6, prompt del ritiro |
| `1036930b7` | **il ritiro**: i due nomi della famiglia testo tolti da `tokens.css` |
| `668b57132` | log del ritiro |
| `2c81335b1` | checkpoint di sessione e prompt Q |
| `e1a65282d` | discovery sui due popup intrappolati sotto il rail |
| `73d30adf0` | **fix**: menu utente e popup notifiche portati sopra il rail |
| `6cb6564a5` | log del fix |
| `19df97e3a` | **D-UI-14**, i due universi degli z-index; `_tmp_` in gitignore |

**Il fatto centrale**: al boot (regime A, nessun `data-theme`) le didascalie dipingevano `#94a3b8`, cioe'
**2.56:1**, sotto qualunque soglia di leggibilita'. Adesso dipingono `#475569`, **7.58:1**, e i 148 siti
di testo di corpo sono passati da `#475569` a `#334155` (10.35:1). Regime A e regime B risolvono
**identici su tutti e sei i nomi** della famiglia, misurati in tre stati diversi.

---

## Decisioni prese

**D-UI-13 Emendamento 3** (`4e6877376`). Misure per la copertura dark: dieci nomi senza consumatori e
non nove; l'inversione di gerarchia quantificata (`--color-text-disabled` che perde il valore chiaro
dentro il dark sta a **7.77:1**, sopra il secondario dark a 7.62:1); `--color-text-placeholder` invece
e' a posto per caso (4.19:1); e la scoperta che **la scala di testo dark non e' quella chiara riflessa**,
il suo secondario sta dove il chiaro mette il tertiary e il suo tertiary dove il chiaro mette il disabled.

**D-UI-13 Emendamento 4** (`c2111b91a`). **Il prerequisito dark cade e l'arco 2 e' archiviato senza
essere eseguito.** Il tema scuro e' sospeso da **R-RAIL-44** dal 2026-08-13, seicento righe piu' su nello
stesso file, e ne' l'Emendamento 2 ne' il 3 se ne erano accorti: D-UI-13 dice «il dark e' un fronte vivo»
ed e' falso da una settimana. R-RAIL-44 dichiara di esistere proprio perche' «senza questa voce ogni
prompt SCSS futuro continua ad aggiungere blocchi dark per abitudine», e il prompt dell'arco 2 aggiungeva
sedici dichiarazioni dark. **Il meccanismo dell'errore conta piu' del suo effetto**: si e' letta la
decisione locale invece del record. Registrata anche una correzione a R-RAIL-44: la sua premessa di fatto
e' parzialmente falsa, `e682047a1` ha chiuso **una porta su tre** e il picker con l'opzione Dark e'
ancora raggiungibile da `GlobalDrawer` e dalla rotta `/settings`.

**D-UI-13 Emendamento 5** (`c2111b91a`). I sette dubbi del censimento decisi col criterio gia' ratificato:
i quattro «dato dipinto del grigio del cromo» sono contenuto e restano a didascalia; i tre «cromo che
pero' e' testo che si legge» vanno a placeholder. Piu' la regola sugli alias: undici restano, solo
`--color-disabled` si risorsa da `--color-text-disabled`.

**D-UI-13 Emendamento 6** (`e17bf3c1e`). Go-ahead al ritiro, **con i due nomi insieme**: togliere solo
`tertiary` avrebbe portato didascalia e corpo allo stesso identico `#475569`. Piu' tre lezioni
dell'esecuzione dell'arco 4, sotto. E l'asserzione che governa il ritiro: **regime A e regime B devono
risolvere identici**, che e' la prima volta che due dei tre regimi convergono.

**D-UI-14** (`19df97e3a`). **Gli z-index vivono in due universi, e quello che conta non e' quello scritto
nei fogli.** `#root` e' `position: fixed`, quindi crea un contesto di impilamento e a livello `body` vale
`auto`, cioe' **0**; il rail non gli sta dentro, gli sta **accanto**, figlio di `body`, a 900. Nessun
numero scritto dentro l'app partecipa al confronto col rail. Regola: chi deve stare sopra il rail deve
essere **figlio di `body`**, cioe' passare da un portale, e prendere il numero dalla scala di livello
body (`#root` 0, `.sim-panel` 850, rail 900, popup portati fuori 1000, modali sopra). Corollario:
**l'arco 8 si sgonfia**, perche' le due scale divergenti vivono tutte e due dentro `#root`.

**Taratura della cerimonia** (discussa, non ancora ratificata in `PROTOCOL.md`). Il processo a prompt
lungo, gate a quattro asserzioni, discovery report ed emendamento e' calibrato sulla zona critica, dove
un errore silenzioso costa giorni. Per il CSS il modo di fallire e' «si vede che e' sbagliato», visibile
e reversibile in un commit: stiamo pagando l'assicurazione della zona critica su un rischio che non e'
quello. **Raccomandazione dell'architetto**: fermare la serie cromatica qui. Degli archi rimasti, 5
(bordi), 6 (sfondi, 226 siti), 7 (ombre e transizioni) sono igiene che nessuno vede, e ora sono capiti e
documentati, che e' il grosso del valore; si fanno quando si tocca quel file per altro. L'arco 8 invece
ha appena trovato un difetto vivo, sotto.

---

## Bug risolti

**Le didascalie illeggibili al boot** (`1036930b7`). Da 2.56:1 a 7.58:1 su 59 siti, e il testo di corpo
da 7.58 a 10.35 su 148. Convergenza 6/6 fra regime A e regime B, misurata in tre stati. Forma forte
dell'asserzione passata cinque volte su cinque: **regime A dopo == regime B prima**, sito per sito,
nessun sito con una terza sorgente. Controllo negativo: il regime B non si e' mosso.

**Icone, scorciatoie e segnaposto sotto soglia** (`85c4398f6`). 30 siti da `#94a3b8` a `#64748b`, cioe'
da 2.56:1 a 4.76:1; le 19 icone a `#94a3b8` non passavano nemmeno la soglia 3:1 del non testo. 16 siti
disabilitati restano a `$slate-400`, esentati.

**D-UI-11 chiusa davvero** (`9139887f1`). Le due linee del rail sul token giusto. L'esecutore ha pero'
corretto la premessa: `.props-header` e' `border-bottom: 0px solid` e `.properties-section-header` e' resa
solo da `PropertiesOverview`, che non ha call site. **Nessuna delle tre regole dipinge**: la divergenza
chiusa era latente, non visibile. Avevo letto il colore senza spessore ne' renderer.

---

## Bug nuovi e todo

**Alta**

1. **CHIUSO** (`73d30adf0`): i due popup sotto il rail. Vedi «Bug risolti» e D-UI-14.
2. **`styles/forms.scss` e' fuori dal sistema dei token e su un'altra palette.** 410 righe, **58 letterali
   di colore contro 4 `var(--)`**, rampa **gray** di Tailwind e non slate, importato da `App.tsx:7`, quindi
   globale e vivo. E' la superficie dove l'utente scrive, e ha gia' fatto perdere un edit corretto
   (`tree-view-sidebar.scss:420`, il placeholder che non arriva al pixel perche' un `#9ca3af` vince).
   Arco a se', da decidere.
3. **Il tema scuro e' sospeso ma selezionabile.** Chiudere le due porte rimaste (`GlobalDrawer`, rotta
   `/settings`) costa un componente e fa combaciare il codice con R-RAIL-44. Non tocca l'harness
   Playwright, che scrive `localStorage.theme` e non passa dal picker.

**Media**

4. **Il censimento dell'arco 3 rivendicava una completezza che non aveva**: §3.1 dice che le 19 icone sono
   «elencate per intero», ma §3.8 non ha la colonna della sotto-etichetta. La ripartizione 16/8/19 e' stata
   riderivata dall'esecutore applicando il criterio nell'ordine, e i totali tornano, ma due scambi
   compensativi darebbero lo stesso totale. **La fonte e' ora la entry di log dell'arco 4.**
5. **Mezza UI non e' raggiungibile da una sonda.** 41 siti su 47 non misurati nell'arco 4, e **zero su 41**
   dei siti «altro» raggiunti nel ritiro: pannelli end-user, console, finestra Jodie, pannello attivita'.
   Prima o poi conviene pagare le ricette di stato invece di riscoprirle ogni volta.
6. **Le due scale z restano divergenti** (deroga di D-UI-13): `--z-tooltip` 1070 **sopra** `--z-modal` 1050
   in `tokens.css`, 1050 **sotto** 9999 in `_z-index.scss`. Dopo D-UI-14 e' **igiene interna a `#root`**,
   con un difetto vero (tooltip sotto modale) che morde solo fra fratelli li' dentro.
7. **Il foglio navbar ha una decina di z-index letterali** tarati su vicini letti una volta e ora stantii
   («above tree view sidebar (z-index: 50)» quando il rail sta a 900).
8. **`StatusBar.scss` ha letterali di colore** (`#f8fafc`, `#e2e8f0`, `#64748b`): stessa famiglia di
   `forms.scss`.
9. **Le custom instructions del Project divergono dal repo**: dicono rotazione del log a 20 entry (P9 dice
   40) e `localhost:3001` (P8 dice 3000). **Solo Alfonso puo' cambiarle**, e ogni chat nuova riparte da li'.
10. Archi 5, 6, 7 di D-UI-13: aperti, ma vedi la raccomandazione sulla taratura.
11. **CHIUSO**: le sonde `_tmp_` restano su disco e sono in `.gitignore` (`19df97e3a`).
12. **CHIUSO**: push fatto, origin allineato a `19df97e3a`.
13. Backlog `2.228` ereditato e invariato; serie D mai arrivata in `decisions.md`.

---

## Documenti aggiornati

- `frontend/src/styles/tokens.css`: due dichiarazioni in meno, sezione vuota rimossa, commento di testa
  aggiornato.
- 24 file di stile e componenti: 47 scambi di token (arco 4).
- `frontend/src/styles/tokens/README.md`: la scala di testo descritta com'e' davvero, cinque gradi.
- `docs/decisions.md`: D-UI-13 con quattro emendamenti nuovi (3, 4, 5, 6).
- `docs/claude-code-log.md`: due entry, piu' rotazione (attive 42 -> 38, archivio 799 -> 803).
- `docs/prompts/`: quattro prompt nuovi, uno dei quali archiviato.

## Prompt generati per Claude Code

| Prompt | Esito |
|---|---|
| `claude_2026-08-21_1520_prompt_ui_N_arco2_copertura_dark.md` | **archiviato, non eseguire** (R-RAIL-44) |
| `claude_2026-08-21_1545_prompt_ui_O_arco4_smistamento_testo.md` | ✅ `85c4398f6` |
| `claude_2026-08-21_1620_prompt_ui_P_ritiro_testo_tokenscss.md` | ✅ `1036930b7`, convergenza 6/6 |
| `claude_2026-08-21_1650_prompt_ui_Q_popup_sotto_rail.md` | ✅ `e1a65282d` + `73d30adf0`, ipotesi confermata nella tesi e falsificata nei colpevoli |

## Prompt pendenti

Invariati: `claude_2026-08-19_2336_prompt_ui_B_palette_rail_sinistro.md`,
`claude_2026-08-20_0025_prompt_ui_C_fase2_property_editor.md`,
`claude_2026-08-14_1530_prompt_J1_walker_jjel_modulo_puro.md`,
`claude_2026-08-18_1656_prompt_2228_fase2.md`.

---

## Prossimi passi

1. **Decidere su `forms.scss`**: 58 letterali su un'altra palette, sulla superficie dove l'utente scrive.
2. **Decidere se chiudere le porte del picker Dark.**
3. **Allineare le custom instructions del Project al repo** (rotazione log 20 contro 40, `localhost:3001`
   contro 3000): solo Alfonso puo' farlo, e ogni chat nuova riparte da li'.
4. Se si accetta la taratura: scrivere in `PROTOCOL.md` che il lavoro puramente cromatico va con prompt
   corto, gate singolo e senza emendamento salvo cambi di decisione.

---

## Info strutturali scoperte

- **`#root` e' `position: fixed`** (`index.scss:31`), quindi un contesto di impilamento che a livello
  `body` vale `auto`. I figli di `body` misurati: `#root` (auto), `.sim-panel` (850),
  `.properties-tree-overlay` (900). E' la scala che decide chi sta sopra chi fra app e rail.
- **`.app-statusbar { z-index: 50 }` e' su elemento statico, quindi inerte**, e il `100` di
  `navbar.scss:170` non e' il valore vivo (vince `var(--z-navbar)` = 950). Erano i due sospetti
  dell'architetto, e la misura li ha scartati tutti e due.
- **Il pattern del portale esiste gia' nel repo**: `Navbar.tsx:1902`, `ReactDOM.createPortal` per il menu
  di overflow delle tab. Non se ne inventa un altro.
- **`styles/forms.scss`**: 410 righe, 58 letterali, 4 `var(--)`, rampa gray (`#9ca3af`, `#6b7280`,
  `#d1d5db`, `#374151`, `#111827`), importato da `App.tsx:7`.
- **`--color-accent` e' dichiarato solo in `styles/tokens/`**: le mappe di `editor-v2/_themes.scss`
  generano `--accent`, che e' un altro nome.
- **La famiglia del testo ha ora un dichiarante solo.** Restano collisi sfondi, bordi, ombre, transizioni
  e la scala z.
- **Il bridge Cowork verso `~/jjodel` scrive ma non cancella**: git non riesce a rimuovere i propri lock,
  quindi da questa superficie **non si committa**. I file si scrivono qui, il commit lo chiude Alfonso.

---

## Cronologia

Aperta sull'arco 2, la copertura dark, che il checkpoint precedente indicava come prerequisito dell'arco
4. Misure fatte, valori derivati, prompt scritto, decisione emendata: un'ora di lavoro pulito su una
premessa morta da una settimana. Se n'e' accorto Alfonso, non il processo, e la decisione che l'arco 2
violava aveva scritto in anticipo il modo esatto in cui sarebbe stata violata.

Poi l'arco 4, che ha smistato 162 siti su tre destinazioni, e il ritiro, che e' il commit che si vede.
Due lezioni dall'esecutore, tutte e due contro l'architetto: un contratto scritto con una grep di prefisso
quando il censimento contava per token esatto, e un censimento che rivendicava una completezza che non
aveva. Nessuna delle due ha rotto niente, e tutte e due sono state trovate perche' qualcuno ha chiesto
**perche'** un numero non tornava invece di farlo tornare.

La domanda che ha cambiato la sessione e' arrivata alla fine: **e' normale passare ore e fiumi di token su
roba che visivamente non ha riscontro?** In parte no. Il riscontro c'e', ed e' quello appena committato,
ma la cerimonia con cui ci siamo arrivati e' tarata sulla zona critica e non sul CSS. Da qui la
raccomandazione di fermare la serie e di tenere solo cio' che ha un difetto osservabile. Cinque minuti
dopo, l'arco che avevo classificato come «morde solo quando qualcuno apre un tooltip sopra una modale» ha
trovato il suo testimone: due popup che finiscono sotto il rail, screenshot alla mano.

**Coda della giornata.** Il prompt Q e' andato per intero, e ha prodotto la lezione migliore: l'ipotesi
era giusta nella tesi e sbagliata in entrambi i colpevoli che nominava. Ha tenuto perche' la Fase 1
chiedeva le catene di antenati complete e non la verifica del sospetto. **Nominare il sospetto va bene
solo se la misura richiesta e' piu' larga del sospetto.**
