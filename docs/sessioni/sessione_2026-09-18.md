# Sessione 2026-09-16 (pomeriggio) → 2026-09-18: quattro corsie chiuse, il KB ricostruito dal repo

Chat di progetto Jjodel Development. Branch di lavoro: `validation-skeleton` in `~/jjodel`, albero condiviso con la corsia paper (MDE Intelligence). Questa sessione non ha toccato `alfonso-frontend-jjtl` né `simulation-engine`. Il file precedente (`sessione_2026-09-16.md`) copre fino al 16 a mezzogiorno; tutto quello che segue è misurato sul repo il 18 sera, non ricostruito dalla memoria della chat.

## Stato a fine sessione

Della fotografia del 16 restavano aperte quattro corsie. Tre sono chiuse e una è a metà con perimetro chiuso. La rotta archi e righe (Fase 2) è stata eseguita il 16 pomeriggio, non aspettava alcun GO. Il giro 1b del Symbol Editor è finito il 18 mattina con la slice 5 e il suo smoke visivo. La corsia JjScript, nata la sera del 16 e non prevista dal KB, ha chiuso quattro corsie interne (L1..L4) con otto commit. La corsia dei quattro difetti minori delle view è partita il 18 sera: A committato e verificato, B in corso. Il simulatore, che il KB dava «da eseguire», ha invece la slice 0 completata dal 14 in tre commit su `simulation-engine`; il prossimo passo è il passo 1 (eventi), lasciato da parte per scelta esplicita del 18.

Stato dei rami, misurato: `validation-skeleton` a `a39a9aa05`, 30 commit avanti su origin (`3e48caed8`), 220 commit oltre il tronco di cui 41 con patch equivalente già su `alfonso-frontend-jjtl` (`git cherry`); `alfonso-frontend-jjtl` a `7bc6c7365`, 7 avanti su origin; `simulation-engine` a `baf7b2b8a`, senza origin, con dieci commit che il tronco non ha (la storia del tronco è stata riscritta dalla corsia release: da mettere nel gate del merge). Albero sporco: i tre file del paper (corsia di Alfonso) e `alert/style.scss` (item B in corso); `.tracer/` non tracciato nella root.

## Decisioni prese

- 2026-09-16. Rotta archi e righe, Fase 2: la precondizione «la riga reference normale dispatcha `CHILD_CONTEXT_MENU`» è risultata falsa (la riga non esiste come nodo, `dbfeb67ac`); le voci «Create edge view» e «Create row view» vivono nei menu del canvas v2 e riusano `createViewInWorkbench` con due rami nuovi (`f554aa5fb`).
- 2026-09-16. Regola del banco di mutazione: un test si giudica dalle mutazioni che uccide, non dalla riga di cui sembra parlare (§5 di `CLAUDE.md`, `43e598404`). Regola della casa delle regole: `CLAUDE.md` ha una casa sola, il tronco (§6.6, `e786d9d8a`); le quattro regole del 16 sono state trasportate e pushate (`48acb79a1`).
- 2026-09-17. Ogni prompt ha un ID `P-YYYY-MM-DD-HHmm` e ogni messaggio su di esso lo porta; una sessione che riceve un ID diverso o assente non agisce (§6.4, `43e7bf3c1` sul ramo, `7bc6c7365` sul tronco). Motivo misurato: un GO di Fase 2 incollato nella sessione sbagliata, con un cambio di scope che nessuno aveva scritto.
- 2026-09-17. JjScript: i `create` M2 consultano il verdetto di uniqueness prima di scrivere (L1); tutte le superclassi si risolvono prima di creare la classe, e una mancante rifiuta l'intero create (L2, all-or-nothing); il sommario numera le righe saltate nello spazio dell'editor, solo a render time (L3); il validatore rifiuta prima del comando 1 anche una superclasse forward, e le dipendenze `superclass` di class, abstract class e interface diventano `required: true` così il waiter le aspetta (L4). Costo dichiarato di L4: una superclasse davvero assente impiega fino a 500 ms a essere rifiutata.
- 2026-09-18. Symbol Editor slice 5: caption per sezione attiva (D8-a), glifo di fallback per la forma condizionale a zero istanze (D8-b), titolo che resta «Custom symbol» (D8-c), taglia manuale per istanza (D8-d), striscia fissa senza layout shift (D8-e). Il campo `when` opzionale su `BorderOverrideRow` è accettato come ticket, non fatto: resta la lista di predicati ri-derivata con test di equivalenza.
- 2026-09-18. Difetti minori view, item C (in prompt, da confermare al primo hard stop): una lista di metaclassi vuota è una modifica incompleta, non un matching; resta nel draft e il gate di commit dei tre pannelli la salta.
- 2026-09-18. Il simulatore si lascia da parte per ora; una sola sessione sull'albero principale oltre a quella di L3, mai tre.

## Bug risolti

- **Nessun gesto creava una view arco o riga.** Voci nei menu del canvas v2 (`f554aa5fb`).
- **Modale del Symbol Editor sotto il rail delle Properties.** Portal su `body` (`bc42b259c`); resta il caso intorno ai 1600px come lane propria.
- **Jjodie timbrava M1 su un metamodello.** Il livello viene dall'editor a schermo, non dall'ultima selezione (`ccd867bda`).
- **JjScript, quattro difetti silenziosi.** Forward reference rifiutato prima del comando 1 (`2b357af17`); il dialogo mostra l'errore dell'executor (`fad85bae5`); duplicati M2 rifiutati con il nome dell'esistente (`09ce4b60c`); superclassi risolte prima del create (`4898aa60f`); superclasse creata nella riga precedente dello stesso script non trovata perché la dipendenza non era `required` e il waiter non la aspettava, gara preesistente resa visibile da L2 (`9345a4046`); righe saltate numerate nello spazio dell'editor (`139350eea`).
- **`closefunc is not a function`** in coda ai creatori classici: guardia sul binding di Escape (`1f3caab09`, item A).

## Bug nuovi e todo

Aperti dalla corsia JjScript (ticket nel log, `df8a30941` e `7e38f7859`): il ruolo `type-reference` (`dependencies.ts:182-196`) ha lo stesso `required: false` della gara appena chiusa e corre verosimilmente la stessa gara (`create enum Color` seguito da `create attribute tint in Pixel type Color`), da confermare con un test sul waiter e non a schermo, candidato L5 con la forma di `9345a4046`; il messaggio lungo di rifiuto sborda dal riquadro rosso del dialogo (cosmetico); le due liste `EXTENDING_ELEMENT_TYPES` in `dependencies.ts` e `scriptValidator.ts` da mettere a fattor comune.

Dalla corsia view, in esecuzione nel prompt `P-2026-09-18-1650`: B `.dialog-header` nascosto dal foglio degli alert; C `metaclasses: []` committato allo spegnimento del wildcard; D il rename inline senza focus (two-phase, `useEffect` su `renamingViewId` che corre con la ref nulla o focus rubato dopo); E il commento di `ObjectNode.tsx:108-110`.

Symbol Editor: il rail delle Properties copre la colonna destra della modale intorno ai 1600px (lane propria); il campo `when` su `BorderOverrideRow`; modalità Sample e marker multipli (D8, D9, ticket propri).

Processo: il log attivo conta 101 entry contro la soglia di 40 di P9, e la corsia JjScript vi ha scritto direttamente mentre view e Symbol Editor usano `docs/log-inbox/`: al ripiegamento le date si interleveranno; il triage è una corsia esclusiva a repo fermo. `.tracer/` nella root va ignorato o spostato.

## Documenti aggiornati

Prompt in `docs/prompts/`: `claude_2026-09-16_1238_prompt_voci_arco_riga.md`, `_1242_prompt_slice4a_famiglia_goal.md`, `_1603_prompt_slice2_border_per_asse.md`, `_2249_prompt_fix_jjodie_scope_level_m1_on_metamodel.md`, `_2301_prompt_casa_delle_regole.md`, `_2327_prompt_jjscript_forward_refs_and_structured_errors.md`, `_2339_prompt_slice4b_guscio_2h.md`, `claude_2026-09-17_1024_prompt_jjscript_silent_defects_duplicates_extends_skipped.md`, `_1048_prompt_regole_su_shape.md`, `_1425_prompt_slice5_preview_multi_istanza.md`, `claude_2026-09-18_1650_prompt_view_quattro_difetti_minori.md`.

Discovery: `discovery_2026-09-16_border_per_axis.md`, `_corner_radius_axis.md`, `_jjodie_scope_level_m1_on_metamodel.md`, `_jjscript_forward_refs_structured_errors.md`, `_precondizioni_fase2_archi_righe.md`, `_rail_modal_stacking.md`, `_rotta_archi_righe.md`, `_rules_editor_format_predicate.md`; `discovery_2026-09-17_jjscript_duplicates_extends_skipped.md`, `_slice5_preview_instances.md`, `_superclass_same_script_race.md`.

Normativi: `CLAUDE.md` §5 (banco di mutazione), §6.4 (Prompt-ID), §6.6 (casa delle regole), su entrambi i rami.

## Prompt generati per Claude Code

Tutti eseguiti: voci arco e riga (`f554aa5fb`), slice 4a (`27f80d1ac`), slice 2 (`3e4f7536f`), fix Jjodie (`ccd867bda`), casa delle regole (`e786d9d8a`), JjScript forward refs lane A e B (`2b357af17`, `fad85bae5`), slice 4b (`b53d2f5dd`), JjScript L1..L4 (`09ce4b60c`, `4898aa60f`, `139350eea`, `9345a4046`), tabella regole su Shape (`e343242bd`), slice 5 (`5c4db90b1`, docs `4f07114f5`). In esecuzione: `P-2026-09-18-1650`, item A fatto (`1f3caab09`, `a39a9aa05`), B in corso.

## Prompt pendenti

`claude_2026-09-14_0140_prompt_sim_slice0_foundations.md` risulta eseguito il 14 (tre commit su `simulation-engine`), non pendente. Da scrivere: passo 1 del simulatore (eventi come istanze M1), quando Alfonso riapre la corsia; L5 JjScript sul ruolo `type-reference`.

## Prossimi passi

1. Chiudere B, C, D, E della corsia view con gli hard stop previsti.
2. L5 JjScript: test sul waiter per il ruolo `type`, fix con la forma di `9345a4046`, unificazione delle due liste, tutto in un prompt piccolo.
3. Le tre ratifiche in `docs/decisions.md` rimaste dal 16 (tipo del viewpoint come non-scelta, tema della form al Data Manager, nascita delle view con domanda esplicita), più quelle di questa sessione (all-or-nothing del create, lista vuota come draft).
4. Gate del merge di `validation-skeleton` sul tronco: 220 commit, 41 già equivalenti, dieci commit di `simulation-engine` che il tronco non ha. Prima voce del backlog dell'harness, non più rinviabile a lungo.
5. Triage del log attivo (101 entry) e regola unica sull'inbox per tutte le corsie.
6. Dal file precedente: verifica del flip dei sottodomini su `app.jjodel.io`; `master` senza `CLAUDE.md`.

## Info strutturali scoperte

`ScriptBlock.tsx` e `ContextMenu.tsx` importano il barrel `joiner` e non caricano sotto vitest (`window is not defined`): la logica testabile va in un modulo puro accanto (`summaryLines.ts`, `superclassResolution.ts` sono gli esempi), mai in un test sul testo sorgente.

`waitForDependencies` in `jjscript/executor/dependencies.ts` aspetta solo le dipendenze `required: true`; un ruolo `required: false` viene risolto prima che Redux abbia propagato l'elemento creato dalla riga precedente. Vale per ogni ruolo, non solo `superclass`.

`.alert-header, .dialog-header { display: none }` in `alert/style.scss:112`: `.alert-header` non ha consumatori, `.dialog-header` ne ha cinque (i dialoghi New View, New Viewpoint, New Transformation, Execute Transformation, Create Project).

Il focus del rename inline è già tentato da un `useEffect` su `[renamingViewId]` (`TreeViewContent.tsx:2265`); il commento a `:1971` assume che il `<SubViewItem>` monti nello stesso commit, lo schermo dice il contrario.

Sul Mac i commit dalla chat si fanno con `osascript do shell script` e `git commit -- <path>`; funziona anche con una sessione Claude Code attiva sullo stesso albero, purché il pathspec sia il proprio.

## Cronologia

16 settembre, pomeriggio e sera. Fase 2 archi e righe eseguita. Slice 4a, 2 e 4b del Symbol Editor. Fix Jjodie. Due regole normative nuove e il trasporto delle quattro sul tronco. Nasce la corsia JjScript dai forward reference.

17 settembre. Prompt-ID normativo. JjScript L1, L2, il report sulla gara. Tabella delle regole su Shape. Prompt della slice 5.

18 settembre, mattina. Slice 5 committata alle 08:20. La chat ricostruisce lo stato dal repo dopo due giorni di KB fermo e corregge due affermazioni sbagliate (Fase 2 «in attesa di GO», slice 0 «mai partita»).

18 settembre, sera. Smoke visivo della slice 5 e chiusura del giro 1b. L4 e L3 di JjScript chiusi, prompt `P-2026-09-17-1024` finito. Prompt dei quattro difetti minori delle view, item A chiuso. Aggiornamento di questo file e dell'indice.
