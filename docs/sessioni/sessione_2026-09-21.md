# Sessione 2026-09-18/21 — parità della view di default, reintegrazione del ramo, riparazione docs

Prompt-ID del giro iniziale: **P-2026-09-18-2219**. Chat di progetto Jjodel, poi due giornate di
aggiornamenti (19 e 21 settembre). Sostituisce `sessione_2026-09-19.md` come `sessione_CORRENTE.md`.
Le sezioni fino a "Aggiornamento 2026-09-19 sera" sono quelle del checkpoint precedente, invariate;
lo stato reale al 21/9 è nell'ultima sezione.

## Stato a fine sessione

Il giro è chiuso sul codice, su `alfonso-frontend-jjtl` nel worktree `/Users/alfonso/jjodel-release`.
Una object view creata in un viewpoint di sintassi nasce con lo stesso chrome del nodo nativo:
bordo 1px sulla famiglia di token `--color-inode-*`, corner radius 8, sfondo, nome a 14px con colore
del nativo, sottolineato con offset 3px, separatore themed. Parità misurata a zero delta con sonda
Playwright su due metaclassi (con e senza attributi), light e dark. Sei commit:

- `400095370` feat(ir): `TextStyle.underline`, `ShapeSpec.cornerRadius`, seme object a parità
- `12ae8c41c` docs: discovery report e due probe
- `6ee6efcd5` feat(ir): colore dell'etichetta e offset nativo della sottolineatura
- `971234d94` docs: appendici della sonda
- `516afd310` fix(ir): le view di default migrate continuano a delegare al nativo dopo il cambio di seme
- `c591cf351` docs: ratifiche R-IRN-29..34 e log entry (in `docs/log-inbox/default-view-parity.md`)

Verifica visiva di R-IRN-34 fatta da Alfonso il 19/9 su un progetto salvato prima di `400095370`:
le view di default migrate rendono ancora via nativo. Chiusa nel registro con `20ddf067e` (vedi
aggiornamento in coda).

## Decisioni prese

Tutte in `docs/decisions.md`, serie R-IRN-29..34, ratificate il 19/9. In sintesi:

- Parità piena del chrome, non solo del box: sottolineatura UML del nome compresa. Ambito: object view.
  Class view fuori. Row view fuori perché `RowViewIR` non ha box. Edge view rinviata a un giro proprio
  con la sua misura (il seme edge `{}` già disegna la freccia aperta: R-8 confermata, verificato a
  runtime, item chiuso come "già lo stato del codice").
- Nessuna migrazione: il seme vale per le view nuove; `VersionFixer` non toccato. Lettura del §7
  dell'addendum TextStyle ir-1.3: seme delle view nuove ≠ default delle view salvate, nessun bump.
- `TextStyle.underline` opzionale, additivo; l'offset 3px del nativo sta dentro il meccanismo di
  underline nel renderer, per scelta e non come default silenzioso.
- `ShapeSpec.cornerRadius` opzionale, a livello di shape e non dentro `border` (il raggio è della
  scatola, non del tratto), `Conditional<number>` in px, fallback non emesso e mai 0, ignorato sulle
  forme senza angoli e su quelle dipinte in SVG; segnalazione in authoring dovuta (R-IRN-3). L'8 del
  seme è accoppiato a mano a `instanceNode.scss`. **Superata il 19/9 sera da R-IRN-35 (vedi coda):
  il ramo aveva già lo stesso asse con un altro disegno.**
- Il criterio del giro è stato ristretto al chrome il 19/9: i delta di geometria (padding e altezza del
  compartimento, separatore su `Empty`, padding dell'header, allineamento del testo) non si chiudono
  dal seme senza assi nuovi o costanti globali retroattive; aprono un giro sui default di layout dell'IR.
- Decisione 4 del prompt (tutto autorabile) lascia dovuta la slice S6: controllo per `underline` nel
  `TextStyleEditor` e per `cornerRadius` nel Symbol Editor.
- `rowSeed` in `EnableIRPanel` resta letterale: il `[]` contro `'*'` è semantica voluta.

## Bug risolti

- **Regressione introdotta e chiusa nello stesso giro** (`516afd310`, R-IRN-33). `VersionFixer` 2.226
  (18/7) ha scritto in ogni view di default M1 uno snapshot di `defaultObjectViewIR()`;
  `isMigratedDefaultView` confronta lo snapshot con la factory corrente per decidere se rendere via
  nativo. Cambiare la factory ha fatto perdere la resa nativa a tutti i progetti migrati dal 18/7.
  Fix: forma vecchia congelata in `LEGACY_OBJECT_VIEW_SNAPSHOT`, riconosciute entrambe. Tre test che
  fissano la regressione (forma vecchia → delega, forma nuova → delega, forma modificata → no), con
  prova per mutazione. I 755 test precedenti non la vedevano perché costruiscono sempre dalla factory.

## Bug nuovi / Todo

- **Alta.** Debito: l'identità della view migrata è per uguaglianza strutturale con un bersaglio
  mobile; ogni futura modifica alla factory richiede una nuova forma congelata. Soluzione da decidere
  in un giro proprio: marcatura al momento della migrazione con stato "non toccato" tracciato.
- **Media.** Giro sui default di layout dell'IR (delta di geometria elencati in R-IRN-29), con la
  discussione sulla retroattività che merita.
- **Media.** S6, controlli di authoring per `underline` e `cornerRadius`.
- **Media.** Giro edge parity con misura propria (stroke, spessore, label) su un arco M1 classico.
- **Media, per il gate del merge.** Su `alfonso-frontend-jjtl` il canvas v2 non crea edge view dal
  menu (`createViewInWorkbench` costruisce solo vertex); il fronte è dato per chiuso su
  `validation-skeleton`. Il lavoro non è mai arrivato sul tronco. Voce inserita nel prompt del gate
  (sezione 6). **Risposta del report: arriva col merge, nessun passo extra.** Arrivato: verificato a
  schermo il 21/9 (step D).
- **Bassa.** Segnalazione in authoring di `cornerRadius` su forma senza angoli (R-IRN-3).
- **Bassa.** Il ritiro di `EnableIRPanel` (R-IRN-5, 13/8) è deciso e mai eseguito: non è un trasporto
  mancato fra rami.

## Documenti aggiornati

- `docs/decisions.md`: R-IRN-29..34, poi riga di chiusura di R-IRN-34 (`20ddf067e`).
- `docs/discovery/discovery_2026-09-18_default_view_parity.md`: Finding 1 corretto con nota datata
  (la delega da `EnableIRPanel` a `irDefaults` esiste dal 23/7 per vertex e dal 28/7 per edge; la
  duplicazione era vera solo per row), Finding 2 corretto (non-bug: entrambi i renderer sopprimono il
  compartimento a zero attributi), due appendici con le misure della sonda.
- `docs/discovery/harness/probe_2026-09-19_ir_vs_native_object_style_parity.mts` e il probe del 18.
- `docs/log-inbox/default-view-parity.md`: entry del giro, `Regressions: yes`, nota di processo sullo
  stash ripreso e rifatto in modo conforme.
- Prompt: `docs/prompts/claude_2026-09-18_2219_prompt_default_view_parity.md` (Fase 1): **committato
  sul tronco il 19/9 alle 17:23 (`20a13bb29`)**; la copia non tracciata in `~/jjodel` va rimossa prima
  di portare `~/jjodel` sul tronco (addendum del 1740, punto 3). **Il file di Fase 2 non è mai stato
  salvato nel repo**: il contenuto è stato incollato in chat e va salvato come
  `docs/prompts/claude_2026-09-18_2219_fase2_default_view_parity.md`.

## Prompt generati per Claude Code

- `P-2026-09-18-2219` Fase 1 discovery: ✅ eseguita nel worktree `jjodel-release`.
- `P-2026-09-18-2219` Fase 2 (S1 underline, cornerRadius, S2 in quattro batch, S3 delega, item 4
  edge, S5 verbale): ✅ chiusa, con S3 e item 4 risultati no-op e S2 batch 4 chiuso su due item su tre.
- `P-2026-09-19-1622` gate del merge, Fase 1 read-only: ✅ eseguita il 19/9 alle 17:19 in
  `~/jjodel-release` (sessione Sonnet 5), report `7f16439ff`, inbox `65c092604`.
- `P-2026-09-19-1735` ratifiche sul tronco (R-IRN-35, R-IRN-36, RC-14, clausola di reintegrazione in
  P14): ✅ eseguita (`ca23ae72a`), vedi aggiornamento del 21/9.
- `P-2026-09-19-1730` corner radius come asse condizionale sul ramo, two-phase: ✅ eseguita sul ramo,
  vedi aggiornamento del 21/9.
- `P-2026-09-19-1740` merge di `validation-skeleton` nel tronco (RC-14): ✅ chiusa in tutti i passi
  A-F, vedi aggiornamento del 21/9.
- `P-2026-09-19-1745`: **superato**, header nel file, non si esegue (`e9e536ace`).
- `P-2026-09-21-1420` riparazione docs dopo il merge: ✅ chiusa, vedi aggiornamento del 21/9.
- Slice S6 (authoring dei due assi): da scrivere, sull'albero unificato.

## Prompt pendenti

Dalle sessioni precedenti, invariati: simulatore passo 1 (eventi come istanze M1); `P-2026-09-18-1930`
split di `CLAUDE.md` oltre il limite (la corsia `P-2026-09-18-2110` sul tronco ne ha fatto una parte:
§18 e §19 in `docs/CODEBASE-MAP.md`); ticket JjScript L5 sul ruolo `type-reference`
(`P-2026-09-18-2055` sul ramo).

## Prossimi passi

Superati dall'aggiornamento del 21/9; la lista corrente è in coda.

## Info strutturali scoperte

- `isMigratedDefaultView` (`irDefaults.ts:142`) è il solo consumatore della delega al nativo
  (`ObjectNode.tsx:116` nativo, `:889` interprete). Ogni cambio alla factory passa da lì.
- Il `+` sul viewpoint crea una view inerte (nessun IR, `jsxString` vuoto); il tipo si sceglie dopo in
  `EnableIRPanel`, che per vertex ed edge spreadda le factory di `irDefaults.ts`.
- Il compile edge (`irCompile.ts:517-519`) riempie `none`/`openArrow` prima che `UnifiedEdge` legga le
  terminazioni; `irMarkerUrl` restituisce `undefined` solo per un valore sconosciuto, mai per assenza.
- `validateIR` non respinge campi sconosciuti su shape e text.
- Il compartimento IR prende il padding da `--ir-pad-*` (`irStyle.ts`), condiviso con l'etichetta, con
  tre taglie fisse; `line-height: 1.4` su `.ir-row` è globale. Nessun asse di layout in `TextStyle`.
- Il nativo disegna la riga sotto l'header anche senza compartimento (`instanceNode.scss:76`); l'IR
  la disegna come `border-top` del compartimento, quindi sparisce con lui.
- Node 26 esegue i probe `.mts` direttamente; `tsx` non è installato.
- `device_commit_files`/bridge: il file di un prompt consegnato solo in chat non arriva sul disco; va
  salvato a mano o la lane non lo trova.
- `git merge-tree --write-tree --name-only A B` e `git merge-file -p` su file estratti con `git show`
  misurano i conflitti di un merge senza toccare l'albero: sono gli strumenti del gate.
- `PROTOCOL.md` P14: il codice passa fra rami solo per `cherry-pick -x` di sha espliciti; dal 19/9
  (1735) P14 ha una clausola di reintegrazione per il merge di un ramo longevo (RC-14).
- Dalla chat via `osascript do shell script`: `git commit -m ... -- <path>` vuole il pathspec **dopo**
  i `-m`, altrimenti i messaggi vengono letti come pathspec; un `--amend` cambia lo sha, quindi un
  prompt non può citare il proprio commit (si cita "il commit che aggiunge questo file").
- I trailer nei corpi dei commit attribuiscono le sessioni: `Model: Anthropic Claude Sonnet 5` senza
  `Claude-Session` = lane Claude Code; `Model: Anthropic Claude Fable 5.1` con `Claude-Session:` =
  chat Cowork, e l'URL distingue le chat fra loro.

## Cronologia

Il 18 sera Alfonso mostra due schermate: la resa povera di una view di default in un viewpoint e la
resa nativa. Chiede che il default coincida con la sintassi astratta. Tre scelte iniziali (parità
piena, object più edge e row, nessuna migrazione), prompt two-phase. La Fase 1 misura sette delta a
runtime e trova quella che sembra una duplicazione del seme in `EnableIRPanel`. Fase 2 scritta con
cinque slice; il file non raggiunge mai il disco. Si scoprono in fila: il compartimento è già
soppresso a zero attributi da entrambi i renderer; `ShapeSpec` non ha un raggio; l'edge non ha
baseline misurata. Si decide `cornerRadius` su shape, non su border; edge rinviata; niente
costante globale. S1 e S2 in tre batch, con una correzione sul fallback (non emesso, non 0). I
crediti finiscono durante il quarto batch. Il 19 una sessione nuova rilegge il disco, committa il
lavoro con pathspec, chiude due item su tre del quarto batch e si ferma sulla geometria. Alfonso
ristringe il criterio al chrome. S3 risulta già vero dal 23 luglio: il Finding 1 era falso. L'item 4
sull'arco risulta già vero e viene verificato a schermo. Prima del verbale la lane scopre che il seme
nuovo fa perdere la resa nativa ai progetti migrati; fix con forma congelata e test per mutazione.
S5 con sei ratifiche. Resta a Alfonso la verifica su un progetto vecchio.

## Aggiornamento 2026-09-19 pomeriggio (chat di ripresa)

Stato reale letto dal repo alle 16:10, non dalla memoria della chat. Rispetto al checkpoint del
mattino: il tronco ha ricevuto la corsia dello split di `CLAUDE.md` (`P-2026-09-18-2110`, commit
`19112458f`..`2b1cc6d05`, intercalati ai sei del giro parità); `validation-skeleton` è a `941a94da9`
con una corsia nuova aperta alle 16:16, `P-2026-09-19-1610` (identità delle metaclassi omonime fra
metamodelli), con `irTypes.ts`, `irResolveCore.ts`, `metaclassPin.ts` modificati e non committati
in `~/jjodel`. Sul ramo, `c66e253fc` ha già misurato con `git merge-file` il conflitto su
`decisions.md` (uno, additivo, testa della serie R-NV).

Misure del gate fatte in chat: 256 avanti e 75 indietro; `git cherry` 41 duplicati e 215 nuovi;
`git merge-tree --write-tree` dà sette file in conflitto, con 1-2 hunk ciascuno.
Trovato che `PROTOCOL.md` P14 ammette solo `cherry-pick -x` di sha espliciti, mai il range e mai
un merge commit: con 215 commit il meccanismo è una decisione di Alfonso.

- R-IRN-34 chiusa nel registro: `20ddf067e` sul tronco (docs, pathspec, trailer `Model:`).
- Prompt `P-2026-09-19-1622` (gate del merge, Fase 1 read-only, in `~/jjodel-release`) in
  `docs/prompts/claude_2026-09-19_1622_prompt_merge_gate_validation_skeleton.md`, commit `2da08a722`.
  Il primo tentativo di commit è caduto dentro un merge altrui in corso nello stesso worktree
  (`origin/staging`, PR #154, conflitto su `claude-code-log.md`), concluso in `d4d9c4323`.
- Lezione di processo: prima di un commit dalla chat controllare `MERGE_HEAD` nel git-dir del
  worktree, non solo `git status --short` vuoto.

## Aggiornamento 2026-09-19 sera: release e rami, il gate deciso

Sessione Cowork (Fable 5.1, `session_019t1ZvzWVFWF3XbvGm3MNtX`) con `~/jjodel`, `~/jjodel-release`,
`~/jjodel-sim` collegati; letture git dalla shell nativa via `osascript`, nessun lock.

**Misure alle 17:15** (coppia `2da08a722` / `92d180708`): tronco 105 avanti, ramo 261 avanti;
`git cherry` 41 duplicati (29 docs, 12 codice tutti `cherry-pick -x` regolari) e 220 nuovi (48
codice, 172 docs); **dieci file in conflitto**, 17 hunk: ai sette del mattino si sono aggiunti
`SymbolEditorModal.scss/.tsx` e `lastViewpoint.ts` dal merge di staging (`7b5f4fd3a`, PR #139, che ha
rifatto due fix già fatti dal ramo il 16/9). `simulation-engine`: dei 38 commit solo **sei sono nuovi**
(slice 0: tre di codice a zero conflitti sul tronco, tre entry di log), gli altri 32 sono copie del
tronco. `origin/staging` e il tag `3.0.0` sono nel tronco.

**Scoperta bloccante.** `ShapeSpec.cornerRadius` esiste due volte con contratti incompatibili: sul
ramo dal 16/9 (`8da572191`, Symbol Editor 1b, D5) come `number` scalare con `roundedPolygonPath` in
`shapeRegistry`, clamp `min(w,h)/4`, guardia in `irValidate`, 169 righe di test, controllo nel
pannello; sul tronco dal 18-19/9 (giro parità, R-IRN-31) come `Conditional<number>` compilato in
`irCompile`, applicato al box e "ignorato sulle forme SVG". In più D1 (bordo per asse) toglie
`CompiledView.border`, che la regola S2 del separatore sul tronco legge fuori dagli hunk. Il merge
testuale nasconde entrambi.

**Fatto in chat.** Push di `validation-skeleton` (71 commit, ora `origin` = `92d180708`) e del tronco.
Il prompt `P-2026-09-19-1622` era stato eseguito alle 17:19 da una sessione Claude Code (Sonnet 5):
report `docs/discovery/discovery_2026-09-19_merge_gate_validation_skeleton.md` (`7f16439ff`, 489
righe, dodici domande aperte), inbox `65c092604`, prompt 2219 Fase 1 committato (`20a13bb29`).

**Decisioni** (Alfonso: push sì, meccanismo "decidi tu"; il resto ratificato col GO ai prompt):
merge commit unico `--no-ff`, **RC-14** (in 1735 come emendamento a P14: clausola di reintegrazione,
non deroga); **R-IRN-35** un solo `cornerRadius`, tipo del tronco (`Conditional<number>`) e semantica
del ramo (poligoni arrotondati, clamp, assente = nessun override), painter che legge il valore
compilato, clausola SVG di R-IRN-31 ristretta alle forme senza angoli; **R-IRN-36** il separatore legge
`borderColor` per asse, `CompiledView.border` sparisce; #139 risolto prendendo il ramo in tutti i sei
hunk con verifica P8; log: unione per heading nel merge, poi corsia P13 con `log:rotate` e
de-duplicazione dell'archivio; simulazione: `cherry-pick -x` dei tre commit di codice, entry di log
in `docs/log-inbox/simulation.md`, tag `archive/simulation-engine-2026-09-14`, ramo riallineato al
tronco; `11bd784b6` (`reminder`) accettato; `HARNESS-DOCS.md` 1.2 atterra stale (ticket `2b1cc6d05`
resta); freeze di entrambi i rami fino al push finale.

**Due chat in parallelo sullo stesso gate.** Questa chat ha scritto `P-2026-09-19-1745` (`7bdf5f638`,
un solo prompt: ratifiche come commit A, trio IR risolto a mano nel merge, RC-14 come deroga). La
chat del pomeriggio "Repository merge operations" (sessione Cowork `012PFkfLpd1nNypWSj7WA1nw`,
segnata chiusa ma ancora in esecuzione) ha letto lo stesso report e scritto fra le 17:36 e le 17:43
la sequenza `1735` (ratifiche, con RC-14 come emendamento a P14) → `1730` (corner radius allineato
sul ramo con test a prova di mutazione, così il merge torna testuale) → `1740` (merge in
`~/jjodel-release` con symlink `node_modules`, gate, rotazione del log, check visivo, push). Stesse
decisioni, sequenza migliore: 1745 è stato superato. Errore di questa chat: `e9e536ace` ha
sovrascritto il 1740 delle 17:43 con una versione parallela; ripristinato in `8b3a21b13` con il testo
originale più un addendum (passo F: slice 0 del simulatore; due controlli visivi in più: R-IRN-34 su
progetto vecchio e R-MCID nel picker; nota sulla copia non tracciata del 2219 in `~/jjodel`).
Lezione: una sola chat possiede un fronte; prima di scrivere un prompt,
`git log -5 --format=%B | grep Claude-Session` (un id diverso dal proprio nelle ultime ore è una chat
concorrente); una chat Cowork "chiusa" finisce comunque il turno in corso nel cloud.

**Ticket nuovi**: raggio a regole senza preview nel Symbol Editor (v1); i sette heading duplicati
nell'archivio del log; `alfonso_dev` con upstream `[gone]` e `staging` locale 1131 dietro (pulizia
cosmetica, nessuna urgenza).

## Aggiornamento 2026-09-21: merge chiuso, riparazione docs, stato reale del tronco

Stato letto dal repo il 21/9 fra le 14:00 e le 14:30 (sessione Cowork Fable 5.1,
`session_01CsANnqfCfL4miZkx94iSZp`, `~/jjodel-release` collegato). Tronco `alfonso-frontend-jjtl`
pushato e allineato a origin su `796458cfa`; `~/jjodel` su `validation-skeleton` a `4d8a93124`
(tracer, 20/9, fuori lane) con WIP non committato sotto `docs/mde-intelligence-2026/` (snapshot
metriche, `main.tex`, `main.pdf`) e la copia non tracciata del prompt 2219; `~/jjodel-sim` riallineato
al tronco a `577cc52b5`.

**La sequenza del 19 sera è stata eseguita tutta**, una lane per volta, sessioni Claude Code Sonnet 5:
`1735` (`ca23ae72a`, ratifiche e P14 1.3 → 1.4), `1730` sul ramo (`cornerRadius` allineato a
R-IRN-35 prima del merge), `1740` in tutti i passi: A misure, B merge `4d397ac02` `--no-ff` (parenti
`1b36576fb` e `30707bfd9`, 228 file, 11 in conflitto, vitest 3935/0, typecheck 14, build ok), C
rotazione `491fc1c4b` (fold di 15 entry da sei inbox, non "sette" come dice il corpo del commit;
archivio de-duplicato 1212 → 1169), D verifica visiva di Alfonso sette su sette su `localhost:3002`,
E push del tronco, F slice 0 del simulatore con tre `cherry-pick -x` (`135ab7a24`, `25cd6149a`,
`857cb9335`), tag `archive/simulation-engine-2026-09-14`, vitest 3962/0. Deviazione accettata da
Alfonso: `irValidate.test.ts` leggeva la chiave dal seed (che ha `cornerRadius: 8`), due righe di
test. Il merge ha lasciato tre residui dichiarati sotto RC-11: Check B rosso (dieci errori di campo in
sette entry ripiegate), R-IRN-35/36 e RC-14 in inglese dentro `decisions.md`, `HARNESS-DOCS.md` a 1.2
con range P1..P12.

**Lane `P-2026-09-21-1420`, riparazione docs, chiusa.** Prompt `f2ef6951f`
(`docs/prompts/claude_2026-09-21_1420_prompt_docs_after_merge.md`, scritto dalla chat
`019t1ZvzWVFWF3XbvGm3MNtX`). Fase 1 eseguita dalla chat Cowork stessa via bridge e shell nativa:
report `245a171a4` (`docs/discovery/discovery_2026-09-21_check_b_after_fold.md`). Risultato
centrale: le sette entry sono tutte del 19/9, post-rule sotto ogni soglia (regola `77edbfbc5` del
1/8, cut-off del gate 2/8 già presente come `LINT_FROM_DATE`, forma stretta `(x)` `c9bd6112a` del
2/9), quindi nessuna modifica al gate. Causa strutturale: Check B non legge mai le inbox e il fold
copia verbatim. Fase 2 eseguita da Claude Code (Sonnet 5, sessione `05d05f8c`) con le sei risposte
di Alfonso (a capo dopo la lettera, sentinella sui due ticket, `Corregge` come `2026-09-18 19:30`
con il nome file in parentesi, accento `è`, riga 374 fuori scope, ticket nella entry): `aa9bcfaad`
log (21 righe, solo byte dei campi), `2e291a46a` traduzione delle tre entry, `877d6febc`
HARNESS-DOCS 1.3 (quattro righe: versione, 122, 344, 358), `796458cfa` entry in
`docs/log-inbox/harness.md` con blocco ticket. Review della chat sui diff e `check:docs` rieseguito:
4/4, `check:agents` verde, albero pulito. Push fatto da Alfonso da terminale.

**Decisioni del 21/9** (Alfonso, con il GO): le entry post-rule si sanano nei soli byte dei campi,
mai nel testo (l'annotazione di `Causa` resta sulla riga seguente, che il parser ignora); i blocchi
ticket prendono le due sentinelle in coda; `Corregge` tiene il nome file come annotazione dopo il
timestamp; le traduzioni usano `è` come le vicine RC-13 e R-IRN-34 e tengono in inglese, fra
parentesi, la frase "Ratified on question N of section 10" e i nomi di clausola citati.

**Ticket aperti il 21/9** (nel blocco della entry di harness): (1) le inbox sono fuori da Check B;
o `check-docs.ts` le linta con le stesse regole del log attivo, o `rotate-log.ts` rifiuta di
ripiegare una entry che fallirebbe; (2) il log non ha un tipo "ticket": i blocchi `## data — ticket`
sono letti dal gate come task. Restano: raggio a regole senza preview nel Symbol Editor; riga 374 e
§4.5 di HARNESS-DOCS stale (Check D e `log:rotate` assenti), per un refresh proprio; identità della
view migrata per uguaglianza strutturale (debito R-IRN-33); S6 authoring dei due assi; giro sui
default di layout; edge parity; il file di Fase 2 del 2219 mai salvato in `docs/prompts/`.

**Stato del log**: attivo a 40 esatti (soglia di Check D); sette entry in attesa nelle inbox
(`merge-gate` 3, `simulation` 3, `harness` 1), tutte in forma valida. Il `Corregge: 2026-09-19 17:40`
della entry di harness dà il warning "well-formed but unresolved" finché `merge-gate` non è ripiegata.
**La corsia esclusiva di rotazione (P13, `npm run log:rotate -- --fold --rotate --write`) è il
prerequisito di qualsiasi lane che scriva nel log.**

**Info strutturali e lezioni del 21/9.**
- Sul Mac il node di default di nvm è la v18 e rifiuta `--experimental-strip-types`; i gate girano
  con `~/.local/bin/node` (v26.8.1). Ogni prompt che esegue i gate lo dichiara.
- Una sessione Claude Code aperta in `~/jjodel` non vede i file del tronco: "non trovo il prompt" con
  ultimo commit del 20/9 e la copia non tracciata del 2219 è la firma dell'albero sbagliato. Il GO
  di una lane sul tronco dice sempre "in `~/jjodel-release`".
- Dalla chat, l'output di `osascript do shell script` perde gli a capo: per copiare verbatim l'output
  di un gate lo si scrive su file in `/tmp` e lo si copia nella cartella collegata, poi si rimuove
  dalla shell nativa prima del commit.
- `parseEntries` del gate tiene solo la prima riga di ogni campo; Check C misura lo span di `Notes`
  fino al campo successivo. Spostare un'annotazione in `Notes` può rompere Check C (misurato: la entry
  Phase 3 avrebbe fatto 224+347 caratteri).
- Il trailer `Model:` scritto con un `-m` separato finisce in un paragrafo proprio: `git` non lo vede
  come trailer (`%(trailers:key=Model)` vuoto) ma il `grep` di P6 lo trova. Per averlo anche come
  trailer git va nello stesso paragrafo di `Co-Authored-By`.

**Prossimi passi (lista corrente).**
1. Alfonso: sostituire `HARNESS-DOCS.md` (1.3) nel Project Knowledge.
2. Corsia di rotazione del log (P13 esclusiva): fold delle tre inbox, rotazione, verifica che il
   warning su `2026-09-19 17:40` sparisca. Prompt da scrivere, breve.
3. Decidere il destino di `~/jjodel`: portarlo sul tronco (rimuovendo prima la copia non tracciata
   del 2219 e mettendo al sicuro il WIP di `mde-intelligence-2026`) o tenerlo per il ramo.
4. S6 (authoring di `underline` e `cornerRadius`) sull'albero unificato, poi il giro sui default di
   layout.
5. Lane sul gate: inbox dentro Check B e tipo "ticket" nel log (i due ticket del 21/9).
6. Alfonso: salvare il file di Fase 2 di `P-2026-09-18-2219` in `docs/prompts/`.
