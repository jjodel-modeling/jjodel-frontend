# Prompt: merge the icons-font and navigate-cancel lanes into the trunk

Prompt-ID: P-2026-09-26-1550
Chat: C-2026-09-25-1353
Lane: full (two merges in sequence, trunk code moved)
Status: eseguito 2026-09-26 · lane merge · ad49c86e2 · verifica visiva passata 2026-09-26

Worktree: `~/jjodel-release`, branch `alfonso-frontend-jjtl`. Before anything else run `pwd` and `git branch --show-current`: if the answer is not `/Users/alfonso/jjodel-release` on `alfonso-frontend-jjtl`, stop. `~/jjodel-icons` and `~/jjodel-open` are read-only here. Do not touch `~/jjodel`, `~/jjodel-sim`, `~/jjodel-gate`, nor the servers on 3000 and 3002. The 3003 server of `~/jjodel-open` is stopped by its own session, not by you.

Single phase, with one hard stop (the visual check). Two merges, one after the other, no source file edited by hand. No conflict is expected on either; a conflict is a stop.

**This file lives on `navigate-cancel`, not on the trunk.** It reaches the trunk with the second merge (precedent `P-2026-09-26-1045`). Two merges never run at once in this tree: if the tree is dirty or a merge is in progress, stop.

## COSA

**Merge 1, `icons-font`** (worktree `~/jjodel-icons`), closed by `P-2026-09-26-1335` with visual check passed on 3005. Commits over the trunk tip `9290c17be`: `ad64eaa34` (report `docs/discovery/discovery_2026-09-26_bootstrap_icons_font_403.md`), `b1ba29157` (`frontend/vite.config.ts`: `server.fs.allow` gets `searchForWorkspaceRoot(__dirname)` plus the real path of `node_modules`, guarded when `node_modules` is missing), `a860c436d` (closure: Status, entry in `docs/log-inbox/harness.md` after the three already there). Its base is the trunk tip, so the merge is a plain `--no-ff` with no conflict; `dist/` was measured byte-identical on the branch.

**Merge 2, `navigate-cancel`** (worktree `~/jjodel-open`), closed by `P-2026-09-25-1905` with visual check passed on 3003. Base `5433451fd`. Commits: `8d6febf5f` (report `docs/discovery/discovery_2026-09-25_navigate_cancel.md`), `47b1f20c7` (Phase 2 prompt), `982581260` (code: `frontend/src/common/navigateReload.ts` new, `frontend/src/common/__tests__/navigateReload.test.ts` new, `frontend/src/common/U.tsx` with `R.navigate` calling `hashReload`), `970529931` (closure: entry and three tickets in `docs/log-inbox/versionfixer.md`, Status of the two 1905 prompt files), `3b7985be4` (visual outcome, F6 raised to medium, new high ticket on the canvas unload warning), and the commit that adds this file. Since `5433451fd` the trunk gained the simulation-engine merge `4b70b5634` (state operator B2, profiles), the 1335 prompt `9290c17be`, and, after merge 1, the icons lane. None of those touches `U.tsx`, `common/`, the 1905 prompt files or `docs/log-inbox/versionfixer.md` (folded to its header in `02f16c829`, unchanged since), so no conflict is expected. The Phase 1 prompt file `docs/prompts/claude_2026-09-25_1905_prompt_navigate_cancel_discovery.md` exists on both sides: the trunk did not change it after the branch point, the branch flipped its Status; the merge carries the flip.

## COME

1. Preconditions, hard stops: `git status` empty in `~/jjodel-release`, `~/jjodel-icons`, `~/jjodel-open`; no `MERGE_HEAD`; `git worktree list` shows `alfonso-frontend-jjtl` only here, `icons-font` only in `~/jjodel-icons`, `navigate-cancel` only in `~/jjodel-open`; the tip of `icons-font` is `a860c436d` with parent `b1ba29157`; the tip of `navigate-cancel` adds this file and its parent is `3b7985be4`. Record the two tips as `<I>` and `<N>`; later steps use them, never the branch names. Measure on the trunk tip, before any merge: `npm run typecheck` (expected 14, the §17 set), `npx vitest run` (expected 4818, the figure of the 1335 closing report; the 9 files red at import), `npm run build` warning lines.
2. Measure merge 1: merge base of `<I>` (expected `9290c17be`, the trunk tip), `git diff --name-only 9290c17be <I>` (expected: the report, `frontend/vite.config.ts`, the 1335 prompt file, `docs/log-inbox/harness.md`), `git merge-tree --write-tree --name-only alfonso-frontend-jjtl <I>` with no conflict. Confirm the branch changes no normative file (`CLAUDE.md`, `AGENTS.md`, `docs/PROTOCOL.md`, `docs/decisions.md`).
3. `git merge --no-ff <I>`. Subject `merge: allow the shared node_modules in the Vite serving list (P-2026-09-26-1335)`; body in the shape of `98ff75a27`: the shas of COSA, the mechanism in three lines (Vite follows the P14 symlink to `~/jjodel`, the allow list holds this tree only, the CSS passes as an imported module and the font does not), the before/after 403→200 of the branch, `Model:` and `Co-Authored-By` trailers. The 3001 server restarts by itself on the `vite.config.ts` change; do not restart it.
4. Measure merge 2: merge base of `<N>` (expected `5433451fd`), `git diff --name-only 5433451fd <N>`, `git merge-tree --write-tree --name-only alfonso-frontend-jjtl <N>` with no conflict. Any conflict, or any file changed on both sides other than by identical content: stop with the list. Confirm no normative file changes.
5. `git merge --no-ff <N>`. Subject `merge: a cancelled reload keeps the tab alive (P-2026-09-25-1905)`; body: the shas of COSA, what the trunk gained since `5433451fd`, the mechanism in three lines (the Navigation API `navigate` event of the reload, its signal aborted inside `reload()` on Stay, `location.replace` plus the flag down in the same task), the probe summary of `982581260` (P1..P5), the three new tickets by title, trailers.
6. Gates on the second merge commit, from `frontend/` (permanent symlink here: never remove it): `npm run typecheck` 14, the §17 set; `npm run typecheck:scripts` exit 0; `npx vitest run` 4823 (4818 + 5), 0 failed, the same 9 files red at import; `npm run build` exit 0 with the same warning lines as step 1; `npm run check:docs` 4/4; `npm run check:agents` green; `npm run check:scripts` 0 hits. Also `git diff 9290c17be HEAD -- frontend/vite.config.ts` equals `git diff 9290c17be <I> -- frontend/vite.config.ts`, and `git diff 5433451fd HEAD -- frontend/src/common/` equals `git diff 5433451fd <N> -- frontend/src/common/`.
7. **Visual regression, hard stop.** Tell Alfonso to hard-refresh `http://localhost:3001/` (Chrome) and check: (a) the Bootstrap Icons render (the 3001 tree uses the P14 symlink, so before merge 1 the font was 403 there too; DevTools Network shows the `.woff2` at 200); (b) open a project, edit the description, click the logo, answer "Stay on page": the tab stays on the project and a second edit is taken; (c) the same with "Leave": the dashboard opens; (d) a healthy project opens with one page load, no automatic reload. Wait. On a failure, report and stop; no revert without instruction.
8. After his OK, one docs commit: this prompt's Status flipped to `eseguito 2026-09-26 · lane merge · <second merge sha> · verifica visiva passata 2026-09-26`, pathspec after `--`, subject `docs: Status flip for the icons-font and navigate-cancel merges (P-2026-09-26-1550)`, `Model:` trailer. No log entry for the merges (precedents).
9. Leave `~/jjodel-icons`, `~/jjodel-open` and both branches in place. Closing report opening with `[P-2026-09-26-1550 · session <id>]`: `<I>`, `<N>`, the two merge shas, gate numbers, the two diff equalities of step 6, visual result, push state without pushing.

Never: `git add .`, `-A`, `-u`, `git stash`, `git reset --hard`, `git checkout -- .`, `git clean`, `--no-verify`, merging a branch name instead of the sha, squash, rebase, resolving a conflict by hand, deleting any cache, killing a server you did not start, removing any worktree, push.

## RIFERIMENTI

- On `icons-font`: `docs/prompts/claude_2026-09-26_1335_prompt_bootstrap_icons_font.md`, `docs/discovery/discovery_2026-09-26_bootstrap_icons_font_403.md`, `docs/log-inbox/harness.md`.
- On `navigate-cancel`: `docs/prompts/claude_2026-09-25_1905_fase2_navigate_cancel.md`, `docs/discovery/discovery_2026-09-25_navigate_cancel.md`, `docs/log-inbox/versionfixer.md`.
- Merge precedents `98ff75a27` (`P-2026-09-26-1045`) and `4b70b5634` (`P-2026-09-26-1240`).
- `docs/PROTOCOL.md` P13, P14, P15; `docs/decisions.md` RC-12, RC-14, RC-17.
