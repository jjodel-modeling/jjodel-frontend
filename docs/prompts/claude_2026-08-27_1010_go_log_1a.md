# GO: chiusura Slice 1a, entry di log

**Data**: 2026-08-27 10:10
**Riferimento**: commit A `d49143031`, B `918b0ec75`, C `99e5efa85`; GO precedente
`claude_2026-08-27_0925_go_fix_1a_form_system_override.md`.

## Verifica dopo il commit C (chat, Chrome di Alfonso, "State Machine v1" / `model_1`, S0)

- **V4** ✅: input `name` 28px, font 13px, padding 4px 8px, radius 4px, bordo 1px; campo 65px
  (labelrow 13 + input 28 + message 16 + gap 8); `.ir-form__summary` 32px; `.ir-field__message` 16px.
- **V6** ✅: con viewpoint "Conceptual Modeling" e `view.ir = {...ir, form: {widgets: {name:
  'textarea'}}}` sulla view `Concept` (`Pointer1787735346550_USER_39`): textarea IBM Plex Mono 12px,
  56px, `resize: none`; label row `name · JjEL · 1..1` con `.ir-field__hint` in mono 10px. IR
  ripristinato dopo la prova (`form` assente nello store, textarea smontata).
- Nota di prova: con il viewpoint "Class Diagram v1" attivo la form di S0 risolve un'altra view e
  l'override su `Concept` non si vede; non è un difetto, è la risoluzione per viewpoint che
  funziona. Da ricordare nelle prove della 1b: prima il viewpoint, poi l'override.

## Cosa fare

Scrivi l'entry cumulativa A+B+C in `docs/claude-code-log.md` (formato di CLAUDE.md, `Notes` sotto i
500 caratteri), con: decisioni A1-A7; token aggiunti; `formSpec` al posto di `form` su
`CompiledView` (nome persistito invariato); NumberWidget e CheckboxWidget scritti da zero e perché;
`theme` senza default nel compile; il quarto sistema di token `--form-input-*` e le regole globali
di `_form-system.scss` e `style.scss:221` sui widget; esito V1-V7 con **V3 dichiarato non
esercitato su modello reale** (unit test, smoke e sonda CSS a markup iniettato); D3 (splitter del
rail sotto la riga dei tab) e D4 (due blocchi del renderer, non riproducibili) a registro; il
difetto pre-esistente di `Info.tsx` sugli slot creati con `addValue`. Attore della verifica: la
chat sul Chrome di Alfonso.

Commit `docs: log form views slice 1a (A+B+C)` con `git add docs/claude-code-log.md` e questo GO
più il precedente se non ancora tracciati (`docs/prompts/claude_2026-08-27_*`). Poi hard stop: la
1b parte da un prompt nuovo dopo il checkpoint di sessione.
