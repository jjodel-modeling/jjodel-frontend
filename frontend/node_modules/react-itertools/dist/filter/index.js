import { map } from "../map";
export function filter(children, fn, options = {}) {
    return map(children, (el) => {
        if (fn(el)) {
            return el;
        }
        return null;
    }, options);
}
