import { each } from "../each";
export function find(children, fn, options = {}) {
    let found;
    each(children, (el, idx) => {
        if (fn(el, idx)) {
            found = el;
            return true;
        }
        return false;
    }, options);
    return found;
}
