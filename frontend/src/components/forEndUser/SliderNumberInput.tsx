import {Keystrokes} from "../../common/U";
import React from "react";
export const todo = true;
/*
interface Props{
    sliderProps: GObject;
    spinnerProps: GObject;
}
export function SliderNumberInput(props: Props){
    return (<>
    <input type={"range"} step={"any"} {...props.sliderProps} className={"value " + (props.sliderProps.className || '')}
           onBlur={e => { setValue(e.target.value); props.sliderProps.onBlur?.(); }}
           onMouseUp={e => { setValue(e.target.value); props.sliderProps.onMouseUp?.(); }} />
    <input className={"spinner"} placeholder={"value"} type={"number"}
           step={step}
           defaultValue={number.value} onBlur={e => {setNumber(e as any, prefix)}} disabled={readOnly}
           onKeyDown={e => {
               if (e.key === Keystrokes.enter) setNumber(e as any, prefix);
               if (e.key === Keystrokes.escape) (e.target as HTMLInputElement).value = '' + number.value; }} />
</>)
}
*/
